# Usado só por "npm run build:firebase" (vite --mode firebase).
# Front-end passa a rodar no Firebase Hosting, mas as funções em api/*.js
# continuam no Vercel — por isso o build precisa saber a URL absoluta da API.
VITE_API_BASE_URL=https://quant-predictor.vercel.app
