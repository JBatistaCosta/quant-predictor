# Contexto do projeto quant-futebol — resumo para Claude Code

## ⏸️ PENDÊNCIA IMEDIATA (retomar daqui na próxima sessão)
**Sync ao vivo de odds (OddsPapi) generalizado e expandido — CONCLUÍDO, testado em produção com dado real.** Pergunta do usuário: "as odds (ao vivo) que são capturadas como geram custo? teria como expandir mercado e ligas mantendo a cota de requisições?". 3 achados reais, confirmados testando em produção (nunca adivinhados), nesta ordem:
1. **`tarefa=odds-sync-diagnostico`** (nova, só leitura/inspeção — mesmo espírito de `af-diagnostico-time`) — 1 chamada real confirmou que `tournamentIds` (plural) aceita vários torneios numa chamada só (`bookmaker`, singular, já era 1-valor-só) e que `verbosity=3` já devolve TODOS os mercados do fixture (99 distintos numa amostra de 2 ligas) — os dois achados que abriram espaço pra generalizar sem gastar mais cota.
2. **Tentei bater as 16 ligas resolvidas numa chamada só (baseado no achado 1) e levei HTTP 400 "Too many tournament IDs... maximum of 5"** — teto real não documentado antes. Corrigido pra lotes de até 5 torneios por chamada (`loteados`, já existia no arquivo).
3. **`tarefaOddsSyncLote`** (nova, substitui a versão 1-liga-por-vez de `tarefaOddsSync`): 1 chamada por (bookmaker × lote de 5), cobrindo TODAS as ligas com `liga_oddspapi_tournament` resolvido (16 hoje: as 6 domésticas + Libertadores/Sudamericana/Copa do Brasil/Série B/Champions League/Copa América/Eurocopa/Copa do Mundo/Club World Cup/Intercontinental Cup) — não mais restrito a `LIGAS_DOMESTICAS`. `extrairLinhasOddsGenericas` (nova) substitui o parser hardcoded (só 1X2/O-U 2.5/BTTS) por um genérico que itera todos os `marketId`, reaproveitando `slugMercadoHistorico`/`slugSelecaoHistorico` (mesmo rótulo do backfill histórico). **2 bugs reais corrigidos de passagem**: o parser antigo só pegava a linha de Over/Under que por acaso aparecesse primeiro na resposta (cada linha de gols é um `marketId` separado com o mesmo nome "Over Under Full Time"), e BTTS nunca funcionava de verdade pro sync ao vivo — `bookmakerOutcomeId` desse mercado não é `'yes'/'no'` (é um ID interno da casa), só a chave do outcome (`outcomeId`, via cache) é estável.
- **Testado em produção com dado real** (`tarefa=odds-todas`, ~32s, dentro do `maxDuration=60s`): **20.549 linhas inseridas numa rodada só** (antes: só 1X2 + Over/Under 2.5 capturados de forma inconsistente pras 6 domésticas, BTTS sempre 0). Confirmado por query: BTTS agora tem odds reais e plausíveis nas 6 domésticas + várias das novas ligas; mercados novos (`first_half_result_1h`, `half_time_full_time`, `correct_score_full_time`, `over_under_1.5/3.5`, etc.) gravando com nomes de seleção corretos (`1_1`/`x_x`/`2_0` etc.). Único erro (benigno, isolado): FIFA Intercontinental Cup (torneio 44289) devolveu 404 "No fixtures found" — sem jogo com linha aberta no momento, não é bug.
- **Custo real mensal**: cron `odds-todas` a cada 3 dias (~10 rodadas/mês), pior caso agora = 4 lotes × 3 bookmakers = 12 chamadas/rodada = ~120/mês pra cobrir as 16 ligas — contra ~180/mês de antes (pior caso) pra cobrir só 6. Cota total é 250/mês.
- **Não fiz** (fora do que foi pedido): não adicionei mais bookmakers em `BOOKMAKERS_ALVO` nem mudei a frequência do cron — qualquer um dos dois multiplica o número de chamadas (ver comentários no código antes de mexer).

**Carteira (Paper Trading) — feature nova implementada e testada em produção (PR #260, merged), mas com cobertura de predição forward ainda MUITO fina — o valor real da feature depende de trabalho futuro fora deste PR.** Pedido do usuário: "implementar uma carteira (paper trading) com as odds (novas do ano de 2026 e as de jogos que ainda não ocorreram) coletadas para os mercados já abordados em treinamento". Diferente da Simulação de Carteira já existente (`/simulacao-carteira`, sempre recalculada do zero sobre histórico JÁ RESOLVIDO), esta é uma carteira com **banca persistente** que aposta de verdade sobre partidas AINDA NÃO disputadas (`matches.status='scheduled'`), usando odds ao vivo reais já sincronizadas (`odds_market`, `snapshot='pre_closing'`, via `tarefaOddsSync`/`tarefaOddsTodas` -- cobria só as 6 ligas domésticas quando esta carteira foi feita, expandido depois pras 16 ligas com torneio da OddsPapi resolvido, ver entrada de "Sync ao vivo de odds" acima) e as predições que já existem pra essas partidas (`predicoes`/`model_predictions`), nos mesmos 3 mercados já cobertos em treino (`MERCADOS_CARTEIRA_SUPORTADOS`: 1X2/over_under_2.5/btts).
- **O que foi feito**: migração (`paper_trading_carteiras`+`paper_trading_apostas`, RLS leitura pública/escrita authenticated, `unique(carteira_id,match_id,selection)` anti-duplicata), tarefas novas em `api/model-maintenance.js` (`paper-carteira-criar/listar/detalhe/apostar/resolver/alternar-ativa/excluir` com auth + `paper-carteira-rodar-todas` sem auth pro cron, mesmo padrão de `odds-todas`/`elo-rotativo`), cron diário novo em `vercel.json` (09:00 UTC, depois do `predict.yml`/sync de odds — **confirmado que o 4º cron não estourou nenhum teto do plano Hobby**, deploy de produção saiu verde), página nova `/carteira-paper-trading` + aba em "Modelos Avançados".
- **Testado em produção de verdade** (não só preview — preview tem SSO do Vercel, `quant-predictor-*.vercel.app` redireciona 302 pro SSO; só o domínio final `quant-predictor.vercel.app` responde direto): criei uma carteira de teste direto no banco (`dixon_coles_v1`/1X2/Brasileirão), rodei `tarefa=paper-carteira-rodar-todas` em produção — **8 apostas reais colocadas** (EV bruto entre 1,02 e ~1,38, teto de exposição de 15% engatilhado corretamente, nomes de time batendo via join), rodada de novo confirmou idempotência (0 apostas novas, sem duplicar), testei o "resolver" inserindo 2 apostas sintéticas numa partida já finalizada (uma seleção vencedora, uma perdedora) — banca creditada certinho (830→850, +10/-10). Carteira de teste e apostas sintéticas apagadas depois (cascade delete confirmado).
- **Achado importante durante a implementação, ainda sem solução**: cobertura de predição forward é MUITO menor que a de histórico resolvido — `predicoes` (pipeline `predict.yml`/`rodar_predicoes.py`) só tem `dixon_coles_v1` gerando previsão pra partidas agendadas (10-ish partidas/dia, só 1X2, ligas 1 e 7 no momento do teste); os modelos de árvore (`catboost_v9`/`xgboost_v9`/`lightgbm_v9`/v10, que já iteram `modelos_ml.TREINADORES` dinamicamente no script) não estão aparecendo em `predicoes` pra partidas `scheduled` — não investigado a fundo (pode ser dataset de treino vazio, exceção engolida pelo try/except por modelo, ou simplesmente não ter rodado ainda pra essas datas específicas; motivo real fica pra próxima sessão se o usuário quiser mais modelos disponíveis na Carteira). `model_predictions` (pipeline Model Benchmarking custom, é de onde vem BTTS) tem **ZERO linhas pra partidas scheduled** — os modelos customizados treinados (`custom_model_configs`) só preveem sobre o conjunto de teste histórico (partidas já resolvidas), nunca sobre fixtures futuras; então uma Carteira em mercado=btts hoje sempre vai dar 0 apostas até esse pipeline ganhar um modo de "prever partida futura" (o botão "Estimar partida" do Treino Customizado já faz isso pontualmente por partida — `tarefaEstimarPartidaCustom` — mas não persiste em `model_predictions` pra a Carteira reaproveitar automaticamente).
- **Falta**: usuário fazer smoke test manual na UI dos botões que exigem login (criar/apostar/resolver individual/pausar/excluir — não testável nesta sessão, sem browser autenticado); se quiser mais modelos disponíveis na Carteira, investigar por que só dixon_coles_v1 aparece em `predicoes` pra partidas scheduled e/ou estender o pipeline de Treino Customizado a persistir previsão de fixtures futuras em `model_predictions`.

**Backfill completo de odds históricas via OddsPapi pras 14 ligas mapeadas — RODANDO DE VERDADE EM PRODUÇÃO (background), ~1.741/22.842 partidas completas (~7,6%) no último check.** Pedido do usuário: "roda o backfill completo pras outras ligas também" (depois de "máximo de mercados possíveis e máximo de temporadas possível de cada torneio"). Script `/tmp/.../scratchpad/backfill_oddspapi.sh` (fora do repo, só na sessão que rodou isso) chama `?tarefa=odds-historico&liga_id=X` em loop pra cada uma das 14 ligas, idempotente (retoma sozinho de onde parou via `match_source_ids`), com detecção de liga travada (pula pra próxima depois de N rodadas sem `restantes_estimado` mudar, retoma na passada seguinte) e log de sucesso/falhas/sem_historico separados. Mantido rodando via checagens periódicas autoagendadas (a cada ~45min, relança se caiu) — **retomar/continuar monitorando numa próxima sessão se ainda não tiver terminado** (log mostra "TODAS AS LIGAS CONCLUIDAS" quando acabar).
- **4 bugs reais achados MONITORANDO O BACKFILL RODAR DE VERDADE em produção (nenhum vinha de teste manual pontual — só apareceram com o volume/tempo de execução real), todos corrigidos**:
  1. **`restantes_estimado` subestimava o que faltava quando havia falha na rodada** (PR #255) — a fórmula contava `processados` (tudo que ENTROU no lote) como se tivesse virado completo, mas partidas que falham (ex.: HTTP 429 de rate limit, comum com mais mercados/casas por chamada) não são marcadas em `match_source_ids` e continuam pendentes — o número ficava "travado" por várias rodadas mesmo com progresso real acontecendo. Corrigido descontando só `sucesso + sem_historico_na_fonte` (os que realmente viram completos).
  2. **Dedup de partida duplicada instável entre chamadas** (PR #256) — a query de partidas locais (`todosOsJogos`, dentro de `tarefaOddsHistorico`) não tinha `.order()`; pra chaves duplicadas (home/away/data/temporada) com `round` nulo dos dois lados (só a Copa Sudamericana tem isso, achado documentado numa sessão anterior), o Postgres não garante a mesma ordem de linhas entre chamadas, então o "representante" escolhido podia trocar de chamada pra chamada — o match_id marcado completo numa rodada não batia com o da rodada seguinte. Corrigido com `.order('id')`.
  3. **Truncamento silencioso de 1000 linhas do PostgREST — causa raiz real do travamento, não só o #2** (PR #258): a busca de partidas já completas (`match_source_ids`, `source=oddspapi_historico_completo`) buscava **todas as 14 ligas numa chamada só, sem paginação** (mesmo gotcha do risco sistêmico já documentado alhures neste arquivo, ver achado do FotMob mais abaixo). O total dessa marcação passou de 1000 linhas durante esta mesma sessão de backfill; qualquer liga cujas linhas completas caíssem fora da "primeira página" ficava com `ja_importados_antes` sempre 0 — reprocessando as mesmas poucas partidas mais recentes pra sempre. Confirmado em produção pra Copa Sudamericana (46) e Copa do Brasil (28) antes do fix, e confirmado destravado depois (Sudamericana: 6 → 140+ completas em ~1h). Corrigido trocando pra `buscarTudoPaginadoIn` (helper já existente, mesmo padrão de `tarefaPartidasFotmob`), escopado só pelos ids da liga atual (mais barato também).
  4. **`vw_cobertura_odds`/`vw_cobertura_odds_bookmaker` ficaram lentas demais pra ler ao vivo** (PR #257) — `odds_market` passou de 1,5 milhão de linhas por causa do próprio backfill, e as views (criadas quando a tabela era bem menor, ver entrada "Consolidação da navegação" abaixo) foram de instantâneas pra ~15s de agregação — a aba "Cobertura de Odds" em Configurações ficava com o spinner preso pra sempre (sem timeout/try-catch no componente também, achado real via print do usuário). Convertidas pra `MATERIALIZED VIEW` (leitura caiu pra ~2ms) com refresh sob demanda (`tarefa=refresh-cobertura-odds` → RPC `refresh_vw_cobertura_odds()`, `REFRESH CONCURRENTLY` pra não travar leitura durante o backfill) via botão "Atualizar" no painel — não colocado em cron por causa do limite de 1x/dia do Vercel Hobby e pra não somar ~15s em cada rodada do backfill que já roda há horas.
- **Ainda por confirmar**: as ligas que não foram testadas manualmente durante o diagnóstico do bug #3 (Champions League, Copa do Mundo FIFA, FIFA Club World Cup, Brasileirão Série B, FIFA Intercontinental Cup) devem se beneficiar automaticamente do mesmo fix — é o mesmo código pra todas as 14 ligas — mas vale um novo check de progresso-por-liga depois que o backfill avançar mais.

**Consolidação da navegação + abas de cobertura de dados em Configurações — CONCLUÍDO (PR #253).** 3 pedidos simultâneos do usuário: (1) "remodelar a barra de painéis e algumas em um único (tal qual um menu de tarefas)"; (2) aba em Configurações mostrando cobertura de estatísticas (stats/xG/FotMob, por temporada por liga); (3) outra aba com cobertura de odds por liga. Escolhido via pergunta direta ao usuário (`AskUserQuestion`, resposta: as duas opções): Cadastro inteiro (Novo Evento/Importar Jogos/Times/Ligas) passou pra dentro de Configurações como abas, e as 4 páginas de modelo (Model Benchmarking/Simulação de Carteira/Treino Customizado/Modelo v9) viraram uma página nova só (`/modelos-avancados`, `ModelosAvancados.jsx`) também em abas — `Layout.jsx` (`GRUPOS_MENU`) ficou só com um item "Modelos Avançados" em vez de 4. Rotas antigas (`/eventos/novo`, `/model-benchmarking` etc.) mantidas de propósito (usadas por back-links de outras páginas, acesso redundante inofensivo).
- Componente `Tabs.jsx` novo (genérico, reusado nas duas páginas — inclusive na Carteira de Paper Trading, ver entrada acima). `Configuracoes.jsx` ganhou 9 abas (`novo-evento`/`importar-jogos`/`times`/`ligas`/`footiqo`/`fotmob`/`oddspapi`/`cobertura-estatisticas`/`cobertura-odds`).
- 2 views novas no Supabase (migration `cria_views_cobertura_estatisticas_odds`, todas `security_invoker=true`): `vw_cobertura_odds`/`vw_cobertura_odds_bookmaker` (% de partidas finalizadas com odds, por liga/temporada/casa — depois convertidas pra materialized view, ver bug #4 na entrada do backfill acima) e `vw_cobertura_estatisticas` (% com `match_stats`/xG/FotMob, por liga/temporada). Componentes `CoberturaOdds.jsx`/`CoberturaEstatisticas.jsx` (accordion por liga, barra de progresso colorida).
- **Limitação conhecida, NÃO corrigida (usuário optou por investigar outra coisa em vez de corrigir isso, ver entrada abaixo)**: a coluna "XG" de `vw_cobertura_estatisticas`/`CoberturaEstatisticas.jsx` só reflete `match_stats.xg` (fonte Understat, só 5 ligas europeias) — NÃO inclui `match_stats_fotmob.xg` (fonte própria do FotMob, cobertura bem mais ampla). Rótulo enganoso pra quem olha o painel achando que é xG "geral". Vale renomear a coluna ou separar em duas quando sobrar tempo.

**Investigação do gap de xG do FotMob na Copa do Brasil + tarefa de enriquecimento FotMob pra todas as ligas — achado real confirmado (limitação genuína da fonte, não bug), enriquecimento disparável pelo usuário via painel (opção "Todas as ligas" adicionada).** Usuário reparou (via print da aba nova "Cobertura de Estatísticas") que a Copa do Brasil tinha FotMob% alto mas xG% quase zero, perguntou de qual fonte vem o xG usado. Investigação (`jsonb_path_exists` no `stats_raw` já salvo, sem gastar cota de API nova): confirmado que a chave `expected_goals` está **genuinamente ausente** no payload bruto do FotMob pra Copa do Brasil/Copa Sudamericana/FIFA Intercontinental Cup especificamente (não é bug do parser, `MAPA_STATS_TIME`/`montarStatsTime` em `api/model-maintenance.js` estão corretos) — a Copa do Brasil não vai ganhar xG mesmo reprocessando. Usuário confirmou a hipótese de que a cobertura do FotMob pra xG/demais estatísticas é ampla nas OUTRAS ligas (confirmado por query: só essas 3 competições têm 0% hard, o resto tem cobertura real) e pediu pra rodar o enriquecimento em todas, não só Copa do Brasil.
- **Bloqueio real pra disparar isso diretamente**: `tarefa=partidas-fotmob` exige `verificarUsuarioLogado` (sessão de usuário de verdade) — diferente de `tarefa=odds-historico`, não dá pra disparar via curl fora do navegador nesta sessão (mesma limitação de auth já documentada alhures neste arquivo).
- **Corrigido (PR #254)**: `ImportacaoFotmob` (`Configuracoes.jsx`) ganhou opção "Todas as ligas vinculadas (N)" no dropdown — quando selecionada, omite `liga_id` da chamada, caindo no caminho que `tarefaPartidasFotmob` já suportava nativamente (processa todas as ligas com crosswalk FotMob de uma vez quando `ligaId` não é passado) mas que o painel nunca expunha — antes precisava clicar liga por liga (14 vezes). **Falta**: usuário efetivamente clicar "Importar do FotMob" com essa opção pra rodar o enriquecimento de verdade (não pude disparar sozinho pela limitação de auth acima).

**Sessão vencida rejeitando importação FotMob/Footiqo mesmo com usuário logado — CORRIGIDO (PR #259).** Reportado pelo usuário: erro "Não autenticado -- faça login antes de disparar" tentando importar FotMob pra Copa do Mundo de Clubes 2025, mesmo logado. Causa: `ImportacaoFotmob`/`ImportacaoFootiqo` (`Configuracoes.jsx`) capturavam `session` uma única vez (do `useAuth()`, no clique) e reusavam o mesmo `access_token` em toda chamada — inclusive nas dezenas/centenas de rodadas de um loop longo (pior ainda depois da opção "Todas as ligas" acima). Um JWT do Supabase expira em 1h por padrão; se já estava perto de vencer no clique, ou vence no meio do loop, o backend rejeita com 401 mesmo com o usuário continuando logado de verdade no navegador (o contexto React só atualiza via `onAuthStateChange`, que pode não disparar a tempo). Corrigido chamando `supabase.auth.getSession()` de novo (renova sozinho se precisar) antes de cada requisição autenticada, em vez de reusar o token capturado no fechamento — a cada rodada em `ImportacaoFotmob`, antes da chamada única em `ImportacaoFootiqo`. `ImportacaoOddsPapiHistorico` não precisou (confirmado que `tarefa=odds-historico` não exige auth no backend).

**Backfill de odds históricas via OddsPapi pras 9 competições sem odds — DESBLOQUEADO E FUNCIONANDO (painel pronto em /configuracoes, backfill completo já iniciado, ver entrada acima).** Pedido do usuário: "máximo de mercados possíveis e máximo de temporadas possível de cada torneio". Continuação direta da pendência de OddsPortal logo abaixo (que ficou bloqueada por anti-bot) — **achado que muda o pipeline inteiro**: `/v4/historical-odds` da OddsPapi é **grátis de verdade** (confirmado no HTML bruto de `oddspapi.io/en/docs/requests-and-quota`: "always free, calls never increment your request count" — só `/v4/fixtures`, 1 chamada por liga cacheada, consome cota real). Isso torna a OddsPapi uma fonte MELHOR que o OddsHarvester/OddsPortal pras 9 competições do gap (sem bloqueio anti-bot, sem scraping) — confirmado que a OddsPapi tem torneio cadastrado pra TODAS elas, mapeado em `liga_oddspapi_tournament`:

| Liga (nosso banco) | tournamentId OddsPapi |
|---|---|
| Copa Libertadores (23) | 384 |
| Copa Sudamericana (46) | 480 |
| Copa do Brasil (28) | 373 |
| UEFA Champions League (19) | 7 |
| FIFA Club World Cup (47) | 357 |
| Copa America (30) | 133 |
| Eurocopa (22) | 1 (UEFA Euro) |
| Copa do Mundo FIFA (27) | 16 (World Cup — NÃO usar id 24660 "FIFA World Cup", que é uma entrada morta sem `futureFixtures`) |
| Brasileirão Série B (34) | 390 (Brasileiro Serie B) |
| FIFA Intercontinental Cup (57) | 44289 |

- **`tarefaOddsHistorico` (`api/model-maintenance.js`) generalizada** (3 PRs nessa sessão: #246, #247, #248) — antes só extraía 3 mercados hardcoded (1X2/over_under_2.5/btts) de UMA temporada (a mais recente); agora itera TODOS os `marketId` presentes na resposta por bookmaker (testado real: Pinnacle sozinha trouxe 90+ mercados distintos pra Libertadores — handicap asiático/europeu em várias linhas, escanteios, cartões, placar exato, 1º/2º tempo, etc.) e processa QUALQUER temporada pendente por chamada, não só a mais recente. Insert é ADITIVO (nunca duplica os 3 mercados originais já gravados antes dessa mudança, nem sobrescreve nada — `odds_market` não tem constraint UNIQUE, então a checagem é feita em memória por `(match_id,bookmaker,market,selection)` antes de cada insert).
- **3 bugs reais achados testando em produção, todos corrigidos**: (1) sem `season` fixo, a ordem crua de `/v4/fixtures` é cronológica ascendente — processava as partidas mais ANTIGAS primeiro, e a OddsPapi não tem retenção de histórico até lá (Libertadores 2019-2022 sempre dá 404 "No historical odds found") — corrigido processando do mais recente pro mais antigo; (2) partida sem histórico na fonte (404 "No historical odds found", resultado ESTÁVEL) ficava tentando pra sempre sem esse fix — agora marca como "completo" mesmo sem gravar odds, contado separado em `sem_historico_na_fonte`; (3) o filtro `round IS NOT NULL` (herdado de uma correção só pro Brasileirão) zerava 5 das 9 ligas por completo (Série B/Copa do Brasil/Club World Cup/Copa América/Intercontinental nunca têm `round` preenchido nessa fonte, e NENHUMA delas tem duplicata de verdade — só a Sudamericana tem) — trocado por deduplicação real em memória por `(home,away,data,temporada)`.
- **Painel novo em `/configuracoes`** ("OddsPapi — Backfill de Odds Históricas"), pedido explícito do usuário, mesmo padrão visual/funcional do painel já existente do FotMob: dropdown de liga (lendo `liga_oddspapi_tournament`), temporada opcional (vazio = todas), loop de rounds automáticos até `restantes_estimado` zerar.
- **Gap conhecido, NÃO resolvido**: Copa America (tournamentId 133) e Eurocopa (tournamentId 1) devolvem `HTTP 404 "No fixtures found"` em `/v4/fixtures`, mesmo com `from`/`to` cobrindo junho-julho 2024 (quando as edições de 2024 aconteceram de verdade — confirmado que as datas locais batem). Catálogo de torneios da OddsPapi não tem uma edição dedicada tipo "Euro 2024", só o torneio genérico recorrente (hoje "dormente", `futureFixtures: 0`). Causa raiz não decifrada — pode ser limitação real de cobertura da OddsPapi pra torneios intermitentes (não teve tempo de investigar `/v4/fixture` por ID individual nem outros parâmetros).
- ~~**Falta**: rodar o backfill de verdade pras 7 ligas que funcionam~~ — **em andamento** desde uma sessão seguinte, ver entrada no topo deste arquivo (14 ligas, incluindo as 5 que já tinham odds domésticas mas ganharam mercados/casas/temporadas extras — Copa América/Eurocopa continuam fora, gap não resolvido, ver acima).

**Pipeline de odds do OddsPortal via OddsHarvester — TESTADO EM PRODUÇÃO, BLOQUEADO POR ANTI-BOT DO ODDSPORTAL (abandonado em favor da OddsPapi, ver pendência acima — não é bug do nosso código).** Primeira corrida real do workflow (`liga=libertadores`, `season=2024`) em 2026-08-14 **falhou**: OddsHarvester navegou até a página de resultados, mas encontrou 0 event rows (`Extracted 0 unique match links`) — o `PageScroller` ficou 4 tentativas sem a altura da página mudar (2021px fixo) e o `CookieDismisser` gastou o timeout inteiro (10s) sem achar nada, padrão típico de página servida em modo degradado por proteção anti-bot, não de scraper mal configurado. Confirmado via pesquisa no GitHub do próprio OddsHarvester: **issue aberta #85 ("Scraping problem", 2026-08-10, 4 dias antes da nossa corrida)** relata exatamente isso — bloqueio anti-bot persistente mesmo com headless+VPN+proxy, sem resposta dos mantenedores. Há também histórico de issues fechadas por "selector mismatch e anti-bot detection" (#66, #68, #69) — ou seja, oddsportal.com vem endurecendo proteção contra scraping com regularidade, e IP de runner do GitHub Actions (cloud IP conhecido) é ainda mais fácil de flagar que uma máquina residencial. **Não vale insistir tentando mascarar/evadir a detecção** (proxy, fingerprint de browser, etc.) — isso é trabalho upstream do OddsHarvester, não deste repo, e o próprio pacote já assume esse risco no README. Enquanto a issue upstream não é resolvida, considerar essa fonte **não confiável pra depender dela** — se quiser tentar de novo, testar manualmente fora do GitHub Actions (IP residencial) antes de reinvestir em CI.
- Corrigido nessa mesma sessão (bug real, independente do bloqueio): o step "Capturar odds do OddsPortal" do workflow fazia `python ... | tee captura.log` sem `set -o pipefail`, então quando o scraper falhava (`sys.exit(...)`, código 1) o step reportava sucesso mesmo assim (exit code do `tee`, não do python) — csv_path saía vazio e o step seguinte quebrava feio com `git add ""` → `fatal: empty string is not a valid pathspec` (exit 128), mascarando a causa real nos logs do Actions. Agora tem `set -o pipefail` + guarda explícita (aborta com mensagem clara se `CSV_PATH` vier vazio) antes de chegar no commit.

**Pipeline de odds do OddsPortal via OddsHarvester — CÓDIGO PRONTO, AINDA NÃO EXECUTADO.** Pedido do usuário: importar odds das competições que hoje não têm nenhuma fonte gratuita (Libertadores, Sudamericana, Copa do Brasil, Brasileirão Série B, Champions League, Copa América, Eurocopa, Copa do Mundo FIFA, FIFA Club World Cup, Intercontinental Cup — ver pendência de "Otimização da captura de histórico de odds" mais abaixo) usando `jordantete/OddsHarvester` (scraper do oddsportal.com via Playwright), gravando em CSV no próprio repo E em `odds_market` no Supabase — sem interesse em publicar/redistribuir os dados. Prioridade explícita do usuário: **odds da Pinnacle** (casa "sharp", referência de mercado eficiente).
- Dois scripts novos, mesma separação já usada pra Footiqo (captura manual -> ingestão separada): `arquivos_do_claude/capturar_odds_oddsportal.py` (sem dependência de Supabase — só roda o OddsHarvester via subprocess pedindo JSON, normaliza pra um CSV "longo" com colunas `league_slug/season/match_link/match_date/home_team/away_team/home_score/away_score/bookmaker/market/selection/odds/period` e grava em `arquivos_do_claude/odds_oddsportal/`; parser de mercado/seleção é genérico — introspecciona as chaves `{mercado}_market` do JSON em vez de hardcodar por mercado, então cobre 1x2/btts/over-under em qualquer linha/handicaps sem código novo por mercado, atendendo o pedido de "outros mercados além de 1x2/ov2.5/btts") e `arquivos_do_claude/ingestao_odds_oddsportal.py` (lê esse CSV, casa por nome de time + data ±3 dias dentro da liga informada, mesmo padrão de idempotência/alias manual dos outros scripts de odds, grava em `odds_market`). `--target-bookmaker` (passthrough do OddsHarvester) permite capturar só a Pinnacle.
- **Achado real, não uma suposição**: das 10 competições sem odds hoje, só 5 têm slug confirmado no catálogo de ligas embutido no próprio pacote OddsHarvester (`oddsharvester.utils.sport_league_constants.SPORTS_LEAGUES_URLS_MAPPING`, inspecionado no código-fonte instalado, não adivinhado) — `copa-libertadores`, `copa-sudamericana`, `brazil-serie-b`, `champions-league`, `world-cup`. As outras 5 (Copa do Brasil, Copa América, Eurocopa, FIFA Club World Cup, Intercontinental Cup) **não estão nessa lista fixa** e o comando `historic` do OddsHarvester rejeita qualquer slug fora dela (valida contra o dict, não aceita URL solta) — pra essas, o usuário precisa confirmar o slug real navegando manualmente `oddsportal.com/football/.../results/` antes de rodar (script aceita via `--liga-slug-custom`, workflow via input `liga=outro`).
- Novo workflow `.github/workflows/importar_odds_oddsportal.yml`: só `workflow_dispatch` (manual, sem cron — scraping via navegador é mais pesado/arriscado que as fontes CSV estático já usadas no projeto), Python 3.12 (exigência do OddsHarvester, as outras dependências do projeto usam 3.10/3.11), instala `playwright install chromium --with-deps` no runner. Inputs: liga (dropdown das 5 confirmadas + "outro"), season, `liga_id_supabase`, markets, `bookmaker_alvo` (default "Pinnacle"), modo (capturar-e-importar / só-capturar / só-importar-de-csv-existente). Commita o CSV gerado de volta pro repo (`git push` dentro do job, `permissions: contents: write` — nenhum outro workflow deste repo fazia isso até agora) antes de importar pro Supabase.
- **Nada disso rodou de verdade ainda** — Playwright Python não estava disponível nesta sessão de sandbox pra testar contra o oddsportal.com real (só a lógica de normalização/parsing foi testada com dados sintéticos refletindo o schema real do OddsHarvester, inspecionado no código-fonte da versão 0.10.0). Falta: usuário disparar o workflow (Actions -> Importar Odds OddsPortal) pra cada uma das 5 ligas confirmadas + temporadas desejadas, revisar avisos de time sem correspondência no log (`ALIASES_MANUAIS` em `ingestao_odds_oddsportal.py` começa vazio, mesma disciplina dos outros scripts) e, se quiser as outras 5 competições, confirmar os slugs do OddsPortal manualmente primeiro.

**Defasagem de stats FotMob em Copa do Mundo/Eurocopa FECHADA + painel de disparo de importação FotMob em /configuracoes — CONCLUÍDO.** Pedido do usuário: "resolve Copa do Mundo e Eurocopa também [na defasagem de stats FotMob]. além disso, inclua em Configurações uma área pra disparar as importações necessárias do FotMob". Achado antes de mexer: `liga_fonte_externa` (sistema='fotmob') não tinha linha pra league_id=27 (Copa do Mundo FIFA) nem 22 (Eurocopa) — únicas 2 ligas gapadas sem crosswalk nenhum (as outras 8 já tinham crosswalk, só cobertura parcial por nome não casando). `tarefa=fotmob-liga-buscar` achou os IDs certos (World Cup=77, EURO=50); `tarefaBackfillFotmobLiga` não serve pra vincular crosswalk numa liga JÁ existente (só cria liga nova), então as 2 linhas foram gravadas direto via SQL. Rodado `tarefa=partidas-fotmob` em produção até `restantes=0`: **Eurocopa 2024 42/51 (82%)**, **Copa do Mundo FIFA 2026 96/104 (92%)** — resto é nome de seleção não casando (mesmo padrão já visto no backfill de corners/xG via API-Football, ex: "Turkey"/"Türkiye"), não é bug.
- Nova seção "FotMob — Importação de Estatísticas" em `src/pages/Configuracoes.jsx`: dropdown só com ligas que já têm crosswalk FotMob, campo de temporada opcional (em branco = todas as pendentes), botão que chama `tarefa=partidas-fotmob` em rounds sucessivos até `restantes` zerar (mesmo padrão de `resetarERecalcularRating` em `Jogadores.jsx`) — cobre tanto temporadas passadas quanto jogos já encerrados da temporada atual com o mesmo controle (o parâmetro `modo=encerradas` do backend já trata os dois casos igual).
- **`tarefaPartidasFotmob` ganhou auth check** (`verificarUsuarioLogado`) — antes só era chamada manualmente via curl por mim, agora é clicável no frontend (`ProtectedRoute` só esconde botão, não impede chamada direta), então precisa validar sessão de verdade no servidor, mesmo padrão de todas as outras tarefas clicáveis do painel.

**Otimização da captura de histórico de odds — fase gratuita CONCLUÍDA, fase paga (OddsPapi) aguardando decisão do usuário sobre prioridade/orçamento de cota.** Pedido: "otimize a captura de histórico de odds das competições cadastradas... dispomos de várias possíveis fontes". Mapeamento completo: das 43 ligas cadastradas, só 6 (as domésticas "grandes") tinham qualquer odds. Fase gratuita (football-data.co.uk, sem chave de API, sem limite de requisição):
- `ingestao_odds_footballdata.py` (5 ligas europeias "grandes") ganhou Championship/Eredivisie/Primeira Liga (`ELC`/`DED`/`PPL` → `E1`/`N1`/`P1`) — cobertura confirmada testando as URLs reais antes de adicionar. Rodado pra todas as temporadas do banco: Championship 1656/1671 (99%), Eredivisie 927/927, Primeira Liga 926/926.
- Novo `ingestao_odds_footballdata_mls.py` (mesmo padrão de `ingestao_odds_footballdata_brasil.py` — 1 arquivo com todas as temporadas, só fechamento, só 1X2): a seção "New League Data" da fonte (`new/USA.csv`) cobre MLS, achado depois do usuário perguntar "essas ligas não estão na football-data?" quando eu já ia partir pra OddsPapi sem checar essa seção direito. MLS 100% coberta agora (0 → 2072/2072).
- **Total: ~63.500 linhas novas em `odds_market`, zero custo de cota paga.**
- **Confirmado que NÃO tem fonte gratuita** (nem na seção principal nem em "New League Data" da football-data.co.uk) pra: Copa Libertadores, Copa Sudamericana, Copa do Brasil, Brasileirão Série B, UEFA Champions League, Copa America, Eurocopa, Copa do Mundo FIFA, FIFA Club World Cup, FIFA Intercontinental Cup (~7.100+ partidas finalizadas sem odds).
- Pra essas, o mecanismo já existe e é genérico por `liga_id` (`api/model-maintenance.js`, `tarefa=odds-historico-descobrir`/`tarefa=odds-historico`, tabela `liga_oddspapi_tournament`) — só nunca foi estendido além das 6 ligas domésticas (`LIGAS_DOMESTICAS`). Achei o `tournament_id` certo pra 8 das 10 competições direto no cache já existente de `/v4/tournaments` (zero custo: MLS=242 -- já resolvido pela fonte gratuita, não precisa mais -- Brasileirão B=390, Copa do Brasil=373, Libertadores=384, Sudamericana=480, Champions League=7, Copa America=133, Eurocopa=1) — 2 ficaram ambíguas (Copa do Mundo: 16 "World Cup" vs 24660 "FIFA World Cup"; FIFA Intercontinental Cup: 44289 vs 24990 "Intercontinental Cup" sem o prefixo FIFA), precisam de checagem manual antes de gravar (regra do próprio projeto depois de 2 mapeamentos automáticos errados no passado).
- **Usuário decidiu**: só gravar os `tournament_id` (zero custo, não dispara nenhuma chamada de fixture/odds) e decidir depois quando gastar a cota de verdade. Gravados em `liga_oddspapi_tournament`: Brasileirão Série B (liga 34 → tournament 390), Copa do Brasil (28 → 373), Copa Libertadores (23 → 384), Copa Sudamericana (46 → 480), UEFA Champions League (19 → 7), Copa America (30 → 133), Eurocopa (22 → 1). Ficam prontas pra rodar `?tarefa=odds-historico-descobrir&liga_id=X` seguido de `?tarefa=odds-historico&liga_id=X` quando o usuário decidir qual priorizar. Copa do Mundo FIFA e FIFA Intercontinental Cup continuam SEM mapeamento (ambíguas — precisam de checagem manual antes: `16`/`24660` e `44289`/`24990` respectivamente).

**Limpeza dos modelos v1-v8 (Model Benchmarking) — CONCLUÍDA, com um incidente no meio corrigido.** Pedido do usuário: "remova os modelos não personalizados, exceto os v9" — as 8 gerações antigas de catboost/xgboost/lightgbm (v1 a v8, incluindo a variante v3B) foram superadas pela v9 (`walkforward_cv_v9.py`) e nunca chegaram a sair de `status='testing'`. Limpeza de dados: `predicoes` zerada (247.434 linhas, 100% v1-v8 + dixon_coles_v1 antigo — tabela inteira, não sobrou v9 nenhum ali, vive em `model_predictions`), `models_registry`/`model_calibration` com as entradas v1-v8 removidas (regex `^(catboost|xgboost|lightgbm)_v[1-8][a-z]?(_calibrado_...)?$`), mais dois achados de limpeza no caminho: um modelo órfão em `model_predictions` ("Catboost Faixa Gols 03-08-26", 21.564 linhas, config já apagada) e 31.563 linhas obsoletas do config "1X2 — Todas as Ligas (Full DB)" (sobra de antes dele mudar pra walk-forward). Limpeza de código: `modelos_ml.PARAMS_DEFAULT`/`FEATURES_POR_MODELO`/`TREINADORES` só têm mais `catboost_xg_regressor_v1`/`catboost_xgot_regressor_v1` (regressores, categoria separada) + v9/v10; `backtest_kelly.py`/`rodar_predicoes.py`/`backfill_predicoes_historicas.py` não precisaram de mudança (já derivam tudo de `TREINADORES` automaticamente); `src/utils/modelosBenchmarking.js` (tela Model Benchmarking) ficou só com `dixon_coles_v1`. **v10 (features prontas — idade/altura titular + capacidade de estádio, PR #173 — ainda não treinado) foi mantido de propósito**, não é sobra, é a próxima geração já preparada. `dados_historicos.FEATURES_V2`..`FEATURES_V8` continuam no código (cadeia de onde `FEATURES_V9`/`FEATURES_V10` derivam, ainda em uso estrutural mesmo sem modelo nenhum registrado nelas diretamente).
- **Incidente no meio do processo**: apaguei por engano `dixon_coles_v1`/`dixon_coles_walkforward_v1`/`stats_glm_v1` de `model_predictions` — o usuário tinha pedido explicitamente pra MANTER esses três (não são "excesso", um é o modelo de produção real, o outro alimenta o mercado de escanteios ao vivo). Sem point-in-time recovery disponível no projeto Supabase, recuperados regenerando a partir dos scripts originais em `arquivos_do_claude/` (`modelo_dixon_coles.py`, `modelo_dixon_coles_walkforward.py`, `modelo_stats_esperadas.py`) — contagens finais conferidas batendo exatamente com o estado original (9.025/10.610/3.066 linhas). De caminho, achado e corrigido um bug real nos três scripts (fixture duplicada pro mesmo `match_id` quebrava o upsert inteiro no Postgres — "ON CONFLICT DO UPDATE command cannot affect row a second time"; `match_stats` também tinha `(match_id, team_id)` duplicado, desalinhando um merge no `modelo_stats_esperadas.py`) — PR #235.
- **Achado à parte, não corrigido**: os três scripts em `arquivos_do_claude/` têm a `service_role key` do Supabase hardcoded como valor default no próprio código-fonte (não só via variável de ambiente) — chave de admin total do banco exposta em texto plano no repo. Vale rotacionar/remover num PR dedicado.

**Backfill de odds históricas (1X2+O-U+BTTS) do Brasileirão 2026 — ODDS 100% CONCLUÍDAS (212/212), mas SIMULAÇÃO DE CARTEIRA em BTTS ainda não vai gerar apostas de 2026: falta previsão do modelo, não falta odds.** Continuação do #226 (BTTS): das 212 partidas finalizadas da temporada 2026, 41 ficavam presas como "sem casamento" em `tarefaOddsHistorico` (`tarefa=odds-historico`) mesmo com dado disponível na OddsPapi — **bug real encontrado e corrigido (PR #228)**: as 41 partidas envolviam só 2 times (Clube Atlético Mineiro, Club Athletico Paranaense); `/v4/fixtures` (endpoint usado no backfill histórico) sufixa a UF no nome ("Atletico Mineiro MG", "CA Paranaense PR") — convenção diferente de `/v4/odds-by-tournaments` (sync ao vivo), de onde vieram os aliases já existentes em `ALIASES_ODDSPAPI`. O sufixo de UF sozinho não quebra pros outros 18 times (é só sufixo a mais no que já bate por substring), mas quebra pra esses 2 porque o nome local ("Clube Atlético..."/"Club Athletico...") tem prefixo sem raiz em comum com a sigla curta da OddsPapi ("CA"). Corrigido com 2 novos aliases, mesmo padrão dos 6 já existentes. Depois do fix, rodado `tarefa=odds-historico&liga_id=1` repetidas vezes em produção até esgotar `restantes_estimado` — **212/212 partidas completas** (1358 linhas em `odds_market` market='btts'), custo real: 41 chamadas de cota da OddsPapi (dentro do orçamento mensal, ~171 já consumidas em agosto por essa mesma tarefa antes da sessão + essas 41 = ~212/250).
- **Modelo custom BTTS pra 2026 — TREINADO, Simulação de Carteira confirmada funcionando (CONCLUÍDO).** `model_predictions` (mercado `btts`) não tinha NENHUMA linha pra liga 1 temporada 2026 (previsão de BTTS só vem do Painel de Treino Customizado, `predict.yml`/`backfill_predicoes_historicas.yml` não cobrem esse mercado). Criada config `custom_model_configs` (`id=18c3af49-d079-4b94-a63f-df9821387c3c`, nome "BTTS Brasileirão 2026") direto via SQL (service role, mesmo efeito de criar pelo painel — sem token de sessão de usuário disponível nesta sessão) + disparado `treinar_modelo_custom.yml` direto pela API do GitHub Actions (mesmo mecanismo do botão "Treinar" do painel). **Importante, achado pela pesquisa antes de treinar**: `mode='walk_forward_cv'` (usado pelo template pronto `btts_xg` da UI) NUNCA cobre 2026 — o único fold persistido (fold_3) tem teste fixo em 2025, hardcoded, independente de `seasons`. Só `mode='simples'` (split fixo treino≤2023/teste≥2024) alcança 2026. Config usada: `mode=simples`, `algorithm=lightgbm`, `league_ids=[1]`, `seasons=2017..2026`, features `xg_bayesiano_*`/`xga_bayesiano_*`/`media_gols_marcados_5j_*`/`media_gols_sofridos_5j_*` (só as 4 de gols sobreviveram ao treino — xG bayesiano ficou de fora, cobertura fraca pro Brasileirão, mesmo padrão já documentado alhures). Treino rodou ~6min (run #28, sucesso), gravou previsão pra 966 partidas de teste (2024+), das quais **210 são a temporada 2026 completa** — todas com odds BTTS reais casando (confirmado por query). **Testado ponta a ponta**: `tarefa=simulacao-carteira&modelo=BTTS Brasileirão 2026&mercado=btts&liga_id=1&temporada=2026` em produção agora retorna candidatos de verdade (1932 brutos, 105 passam EV≥1.02, 103 apostas simuladas no snapshot de fechamento). **Resultado honesto, sem inflar**: log-loss 0.697 (baseline "sempre 50%" é 0.693 — praticamente empate técnico, modelo não aprendeu sinal real), accuracy 48,9%, e a simulação de carteira desse modelo fecha com **ROI -36,5%** no período de teste (2024+) — não há evidência de edge nesse modelo, consistente com o achado geral do projeto de que os modelos não batem o mercado em mercados binários sem calibração. Documentado como está — decisão de usar/não usar esse modelo pra apostar de verdade é do usuário, não foi tomada aqui.

**Ingestão dos dumps locais do FotMob (`ingestao_fotmob_dumps_locais.py`) — bug real encontrado e corrigido, CONCLUÍDA (sem trabalho novo a fazer por ora).** Usuário disparou o workflow (`ingerir_fotmob_dumps_locais.yml`) e reportou "deu certo" — mas o log mostrava "300 partidas processadas, 0 falhas" enquanto o banco (`match_source_ids`, `match_player_stats_fotmob` etc.) não ganhava NENHUMA linha nova (confirmado via `created_at`: nenhuma linha criada no dia da execução, apesar do log de sucesso). Investigação (com um debug temporário rodado ao vivo no GitHub Actions, removido depois): **bug real em `_paginar_ids_existentes`** — paginava `match_source_ids` (>15k linhas) com `.range()` mas SEM `.order()`, e o PostgREST não garante travessia estável entre chamadas `.range()` sucessivas sem ordenação explícita numa tabela desse tamanho — algumas linhas já sincronizadas (ex.: match_id 6951-6953, de 18/07) ficavam de fora da contagem silenciosamente, aparecendo como "pendentes". O script então reprocessava (upsert) partidas JÁ prontas — como upsert só atualiza uma linha existente, `created_at` nunca mudava e o total da tabela ficava estático, sem nenhum sinal de erro nos logs (sempre reportava sucesso, mascarando o problema completamente).
- Corrigido: `.order(coluna)` adicionado antes de `.range()` em `ingestao_fotmob_dumps_locais.py` E no mesmo padrão em `ingestao_fotmob.py` (2 ocorrências, mesma tabela `match_source_ids` + `matches`).
- **Depois da correção, rodado de verdade**: "Já sincronizados (pulados): 15.951. Pendentes: 0" — ou seja, **as 15.951 partidas do `repository-fotmob` já estavam TODAS sincronizadas** pelo scraper ao vivo (`ingestao_fotmob.py`, via `temporadas_fotmob_backfill.yml` e afins) antes mesmo desta sessão começar. Não havia trabalho novo real — o "4.965 pendentes" de antes era inteiramente um falso positivo do bug de paginação.
- **Risco sistêmico encontrado, NÃO corrigido em todo o projeto** (fora de escopo por ora, avaliar depois): o mesmo padrão (`.range()` sem `.order()`) aparece em ~25 outros pontos do código (`arquivos_do_claude/*.py`, `scripts/dados_historicos.py`, `scripts/backtest_kelly.py`, etc.) — a maioria pagina resultados de `.in_(lote_de_ids)` (lotes pequenos, ids já conhecidos, risco bem menor) mas alguns (ex.: `.eq("league_id", X).range(...)` sem filtro de id) têm o mesmo perfil de risco que causou este bug. Não foi tratado agora por ser um cleanup grande e separado — só os dois arquivos diretamente envolvidos neste bug foram corrigidos.
- Script fica pronto e correto pra uso FUTURO: se o `repository-fotmob` ganhar partidas novas (via `dump_matchdetails.py` rodando lá) que o scraper ao vivo daqui ainda não tenha sincronizado, ou se quiser reprocessar dados existentes com `--forcar` (ex.: pra pegar campos de `stats_raw` que o parser atual não promove a coluna, ver `catalogo_estatisticas_disponiveis.md` seção 6.1), o pipeline está pronto e testado.


**Ingestão dos dumps locais do FotMob (`jbatistacosta/repository-fotmob`) — código pronto e validado, AINDA NÃO EXECUTADA.** Usuário mantém um repositório irmão (`repository-fotmob`, público) só com os payloads `matchDetails` já baixados e gzipados (`data/{league_id}/{season}/{match_id}.json.gz`, IDs internos do quant-predictor) — 15.753 arquivos, 8 competições, 2019-2026 (Brasileirão/Libertadores com histórico mais completo que o banco tinha até agora). Pedido do usuário: importar os dados de jogador completos daí pro banco.
- Refatorado `arquivos_do_claude/ingestao_fotmob.py`: extraída a função `processar_matchdetails_completo(d, match_id, fotmob_match_id, fotmob_to_internal)` do corpo de `main()` — mesma lógica de parse (stats de time, contexto, dimensão de jogador, escalação, stats por jogador, chute a chute), reutilizável tanto pelo scraper ao vivo quanto pelo novo script de dumps locais, sem duplicar/arriscar divergir.
- Novo `arquivos_do_claude/ingestao_fotmob_dumps_locais.py`: lê os `.json.gz` de uma pasta local (`--pasta-dados`), ZERO chamada de rede ao FotMob (todo scraping já foi feito no outro repo). Como o `match_id` já vem do nome do arquivo, resolve o crosswalk time FotMob->interno SÓ com os 2 times daquela partida (compara `general.homeTeam.id`/`awayTeam.id` do JSON com `matches.home_team_id`/`away_team_id` já conhecidos) — não precisa de `team_source_ids` global. Idempotente via `match_source_ids` (mesma convenção). Processa em lotes na memória (`--lote`, default 200) antes de gravar, bem mais rápido que o padrão "1 partida = 1 upsert" do scraper ao vivo (que existe só por causa do pacing de API, que não existe aqui).
- Novo workflow `.github/workflows/ingerir_fotmob_dumps_locais.yml`: `workflow_dispatch` manual, faz checkout dos DOIS repositórios (quant-predictor + repository-fotmob num subdiretório, com `token: ""` de propósito — o GITHUB_TOKEN padrão só tem escopo pro repo atual, falharia com 403 tentando clonar um repo diferente; `repository-fotmob` é público, clone sem autenticação funciona).
- **Validado sem tocar o banco**: rodei `processar_matchdetails_completo` contra os 15.753 arquivos reais do repositório (clonado nesta sessão) — **zero erros de parse**. Testei também o script de ingestão inteiro (batching, dedup, upsert) com um cliente Supabase falso simulando o banco — fluxo completo correto (players/lineup/player_stats/shots/match_stats/contexto/source_ids todos populados nas contagens esperadas).
- **Só falta disparar o workflow** (`workflow_dispatch` no GitHub, aba Actions) — esse ambiente de sandbox não tem como rodar de verdade (mesma limitação de sempre, client Python `supabase` não instala aqui), mas o workflow reproduz no runner do GitHub Actions, que tem ambiente Python limpo — não deveria ter esse problema lá.

**Nova camada de features V8 (estatísticas do FotMob) + tuning real de profundidade de aprendizado — código pronto, AINDA NÃO TREINADO.** Usuário pediu modelos com mais estatísticas de FBref/FotMob + capacidade de escolher a profundidade de aprendizado pelo menor log-loss, evitando overfitting. Levantamento completo de cobertura por liga/temporada em `arquivos_do_claude/catalogo_estatisticas_disponiveis.md` (PR #157). Duas decisões do usuário: (1) só as colunas do FotMob com cobertura quase completa entram na V8 (ficam de fora `touches_opp_box`, cobertura irregular; `accurate_passes_total`, praticamente nunca populada; `xg`/`xgot`, já usados/irregulares no FotMob) — 22 colunas (`total_shots`, `shots_on_target`/`_off_target`/`_blocked`/`_inside_box`/`_outside_box`, `big_chances`/`_missed`, `accurate_passes`/`_long_balls`/`_crosses`, `tackles`, `interceptions`, `blocks`, `clearances`, `keeper_saves`, `duels_won`, `aerial_duels_won`, `successful_dribbles`, `fouls_committed`, `yellow_cards`, `red_cards`, `corners`, `possession`) viram forma pré-jogo (`COLUNAS_STATS_FOTMOB` + `_colunas_forma_fotmob` geram os dicts em loop, em vez de 22 escritos à mão) — sufixo `_fm` nas colunas de saída pra nunca colidir com as equivalentes do FBref (V7). `FEATURES_V8` = V7 + essas 88 colunas (marcado/sofrido × casa/fora × 22). (2) Tuning por grid search + early stopping real (recomendado, escolhido pelo usuário) — implementado em `modelos_ml.py`: `treinar_catboost`/`treinar_xgboost`/`treinar_lightgbm` agora treinam até um teto alto (2000 iterações) e PARAM DE VERDADE (`early_stopping_rounds=50`) no log-loss de validação quando `val_df`/`test_df` são passados (só acontece via `backtest_kelly.tunar_treinar_e_calibrar`) — antes só registrava a curva de loss sem nunca agir sobre ela (iterações sempre fixas em 200/80). Cuidado importante: os 3 frameworks decidem a parada olhando eval_sets DIFERENTES (CatBoost = primeiro, XGBoost = último, LightGBM = qualquer um que pare de melhorar) — corrigido caso a caso pra nunca deixar o Test Set influenciar a decisão de parada (vazamento). Sem holdout (treino diário de produção, `rodar_predicoes.py`), mantém o teto fixo de sempre (`ITERACOES_PRODUCAO`), sem mudança de comportamento lá. **Bug real encontrado e corrigido de passagem**: `catboost_v6`/`v7` (e agora `v8`) nunca tinham entrado em `GRADE_HIPERPARAMETROS` (`backtest_kelly.py`) — a lista de sufixos era fixa (`_v2.._v5/_v3b`) e ficou pra trás; o tuning desses modelos ia estourar `KeyError` silenciosamente (só não quebrava o backtest inteiro por causa do try/except por modelo). Agora `GRADE_HIPERPARAMETROS` é derivado de `modelos_ml.TREINADORES` automaticamente, então v9/v10 futuros herdam a grade da v1 sem precisar lembrar de atualizar essa lista de novo. `catboost_v8`/`xgboost_v8`/`lightgbm_v8` registrados em `TREINADORES`/`PARAMS_DEFAULT`/`FEATURES_POR_MODELO`. **Só treina de verdade quando `backtest_kelly.py` rodar** — mesma limitação de sempre, sandbox sem client Python `supabase` instalável; usuário precisa rodar localmente.

**Nova camada de features V7 (estatísticas de jogo do FBref) — código pronto, AINDA NÃO TREINADA.** Usuário notou que "poucas estatísticas dos jogos estão sendo analisadas" — investigação confirmou: a tabela `match_stats` (FBref, `ingestao_stats_fbref.py`) guarda `possession`/`shots`/`shots_on_target`/`corners`/`fouls`/`yellow_cards`/`red_cards` por partida/time (24.248 linhas, boa cobertura em 14 competições, majoritariamente 2022-2026), mas `dados_historicos.py` só lia a coluna `xg` dessa tabela — as outras 7 nunca tinham virado feature de nenhum modelo. Implementado como camada NOVA (`dados_historicos.FEATURES_V7` = v6 + forma pré-jogo dessas 7 estatísticas) em vez de mexer em v5/v6/v3b — mesma razão de sempre (modelos já registrados com métricas dependentes do shape de feature). Forma calculada com a mesma função genérica já usada pra gols/xG (`_forma_por_mando` — média móvel 5 jogos, `.shift(1)` antes do `.rolling()` pra nunca vazar o próprio jogo, separada por mando casa/fora), nova função `_anexar_stats_extra_por_partida` busca os dados brutos de `match_stats`. 28 novas colunas (marcado/sofrido × casa/fora × 7 estatísticas). `catboost_v7`/`xgboost_v7`/`lightgbm_v7` registrados em `modelos_ml.TREINADORES`/`PARAMS_DEFAULT`/`FEATURES_POR_MODELO` (mesmos defaults sem tuning da v1). **Só treina de verdade quando `rodar_predicoes.py` ou `backtest_kelly.py` rodar** — mesma limitação de sempre, esse ambiente de sandbox não consegue instalar o client Python `supabase` (cadeia de erros PyJWT→cryptography (panic Rust)→`deprecation` faltando no `postgrest`) pra rodar esses scripts aqui; usuário precisa rodar localmente. Depois disso, `sync_models_registry.py` pega as métricas e registra como `testing`.

**Nova camada de features V6 (progresso da temporada) — código pronto, AINDA NÃO TREINADO.** Pedido do usuário: coluna 0-1 representando o quão avançada está a temporada (início vs reta final) + reaproveitar a posição na tabela já existente (`posicao_home`/`posicao_away`, já fazia parte da v5 desde antes). Implementado como camada NOVA (`dados_historicos.FEATURES_V6` = v5 + `progresso_temporada`) em vez de mexer em v5/v3b direto — essas já têm modelos registrados em `models_registry` com métricas de teste que dependem do shape de feature exato, mudar retroativamente invalidaria a comparação. `progresso_temporada` = posição da partida na ordenação por data dentro de (league_id, season), normalizada 0-1 — não usa `round` (só populada a partir de 2023 em várias ligas) nem `jogos_disputados` (não comparável entre ligas com número de rodadas diferente). Usuário decidiu explicitamente NÃO derivar uma feature combinada de "importância da partida" (existe uma heurística assim em `AnaliseEstatisticaJogo.jsx`, `calcularImportanciaPartida()`, mas só pro frontend) — prefere deixar o modelo aprender a interação entre posição e progresso sozinho.
`catboost_v6`/`xgboost_v6`/`lightgbm_v6` registrados em `modelos_ml.TREINADORES`/`PARAMS_DEFAULT`/`FEATURES_POR_MODELO` (mesmos defaults sem tuning da v1, igual todas as versões anteriores no dia em que nasceram). **Só treina de verdade quando `rodar_predicoes.py` ou `backtest_kelly.py` rodar** (scripts Python, não rodam automaticamente aqui) — depois disso, `sync_models_registry.py` pega as métricas e registra como `testing` (mesma regra automática de sempre: só vira `active` se tiver ROI significativo, o que nenhum modelo tem ainda).

**Buraco de odds 2018-2022 nas 5 ligas europeias FECHADO — CONCLUÍDO.** Descoberto ao checar cobertura: as partidas de 2018-2022 (e 2026, temporada futura ainda sem odds em nenhuma fonte) já existiam no banco, mas `odds_market` só tinha 2023/2024/2025 — nenhum motivo aparente, só nunca tinha sido rodado pra essas temporadas mais antigas. Reaproveitado exatamente `ingestao_odds_footballdata.py` (mesma lógica/aliases, já validado) contra os arquivos locais baixados no PR anterior (`arquivos_do_claude/football_data_co_uk/principais_ligas/`) em vez de rebaixar da fonte. **132.245 linhas gravadas** em `odds_market` (`bet365`/`pinnacle`/`william_hill`/`media_mercado`, mercados 1X2 + over/under 2.5) cobrindo Premier League/La Liga/Serie A/Bundesliga/Ligue 1 × 2018-2022 (Ligue 1 2019 tem só 279/380 partidas — a própria liga foi encerrada antecipadamente por causa da Covid, não é bug). Casamento quase 100% (só 2 nomes precisaram do fallback "fuzzy" já existente no script — "Bayern Munich"/"FC Bayern München" e "St Etienne"/"AS Saint-Étienne", ambos só diferença de acento, sem ambiguidade real). Cobertura de odds agora: **2019-2025 completo** nas 5 ligas europeias (2018 só existe pra Bundesliga, que é onde a temporada 2018 tinha partida no banco).

**Buraco de odds do Brasileirão 2025 FECHADO — importado via football-data.co.uk ("Extra Leagues", `new/BRA.csv`, 2012-2026 num arquivo só) — CONCLUÍDO.** Descoberta pelo usuário depois de considerar (e descartar, por não ter odds históricas) o FootyStats. Mesma fonte já usada pras 5 ligas europeias, formato ligeiramente diferente (arquivo único com todas as temporadas via coluna `Season`, só odds de FECHAMENTO — confirmado em `notes.txt`: sufixo "C" = closing). Mapeamento validado por DATA real (±3 dias, mesma tolerância do script europeu) + nome: **100% de casamento em 2019-2026** (2.849/2.849), zero divergência de placar real (as 2 flagadas eram só jogos ao vivo/em andamento no momento da consulta). 2012-2018 (2.660 linhas) ficam de fora — Brasileirão só existe no nosso banco a partir de 2019, não é bug. **19.890 linhas gravadas** em `odds_market` (`bet365`/`pinnacle`/`betfair_exchange`/`media_mercado`, `snapshot='closing'`) — **2025 agora tem odds reais** (`media_mercado`/`betfair_exchange` completos 1140/1140, `bet365`/`pinnacle` parciais — a própria fonte não tem 100% de cobertura por casa nessa temporada, não é bug nosso). Script: `arquivos_do_claude/ingestao_odds_footballdata_brasil.py`.
- 5 aliases achados na validação (mesmo padrão de sempre): `athletico pr→Club Athletico Paranaense`, `atletico go→AC Goianiense`, `atletico mg→Atletico Mineiro`, `botafogo rj→Botafogo FR`, `csa→CS Alagoano`.

**Odds do Brasileirão 2019-2024 importadas via dataset Kaggle (`brunobchinaglia/bets-on-soccer-2015-2025`, arquivos_do_claude/kaggle/) — CONCLUÍDO.** Usuário forneceu credencial do Kaggle na sessão; 15 CSVs de odds (2015-2025, 15 ligas) baixados e guardados em `arquivos_do_claude/kaggle/`. Mapeamento validado ANTES de gravar qualquer coisa (mesma disciplina de sempre): 2.274/2.274 (100%) das partidas do Brasileirão 2019-2024 casaram por `(temporada, time_casa, time_fora)` — chave única dentro de uma temporada de returno duplo, o CSV não tem data — com ZERO divergência de placar entre CSV e banco nas casadas. 2015-2018 (1.425 linhas) ficam de fora pra sempre — Brasileirão só existe no banco a partir de 2019, não é bug. Gravado em `odds_market` com `bookmaker='kaggle_oddspedia'`/`snapshot='closing'` (timing real desconhecido — melhor suposição, documentado no script). Script: `arquivos_do_claude/ingestao_odds_kaggle_brasileirao.py`.
- **3 times duplicados reais encontrados e corrigidos durante a validação** (mesma classe do bug de identidade do início do projeto): América-MG (id 807→3 "América FC"), Sport Recife (id 727→42 "SC Recife"), CSA (id 710→854 "CS Alagoano") — cada duplicata tinha poucas partidas (8-15) sem `external_id`, criada por um fluxo diferente (API-Football) do que populou o resto do Brasileirão (football-data.org). Achados porque essas 3 equipes apareciam como "sem casamento" no CSV mesmo com nome batendo — investigação mostrou que cada temporada já tinha 20 times distintos (round-robin completo), então a partida EXISTIA, só atribuída a um team_id errado.
- **Ainda pendente**: os outros 14 CSVs do Kaggle (Argentina, Bélgica, Colômbia, Eredivisie, Itália, La Liga, Portugal, Ligue1, MLS, Premier League, Rússia, Uruguai, Venezuela + Bundesliga) — 7 ligas domésticas não têm nem liga cadastrada em `leagues` ainda (Argentina, Bélgica, Colômbia, Rússia, Uruguai, Venezuela, MLS); as europeias de elite já têm fonte melhor (football-data.co.uk, múltiplas casas). Ver `arquivos_do_claude/kaggle/README.md`.

**Backfill de escanteios/xG via `sync-match-stats.js` pras 7 competições fora do Brasileirão — ENCERRADO POR ORA, atingiu o teto do que o matching automático consegue.** Usuário contratou o plano pago da API-Football (7.500/dia, 300/min) — bugs de ambiguidade corrigidos no caminho (times homônimos, PR #48; ligas homônimas — 3 ligas chamadas "Championship" na API, PR #54; apelidos sem raiz comum "QPR"/"West Brom", PR #83, e "Sheffield Utd", PR #85 — ver `ALIASES_MANUAIS` em `sync-match-stats.js`) e o pacing foi liberado (PR #53). Rodado em 2 dias (1º dia bateu cota diária, 2º dia rodou TODAS as combinações pendentes até o fim sem bater cota de novo — ou seja, o que sobrou não é mais problema de cota, é problema de nome não casando). Status final (jogos com escanteios OU xG preenchidos / total):
- **Eredivisie: 306/306 nas 3 temporadas (2023-2025) — 100% CONCLUÍDO.**
- Copa do Mundo FIFA 2026: 95/102, Eurocopa 2024: 46/51 — praticamente completos (resto são seleções com nome divergente, ex: "Turkey"/"Türkiye").
- Champions League: 2023 93/125, 2024 164/189, 2025 64/189 (2025 tem vários clubes novos na competição cujo nome não está casando).
- **Copa Libertadores: 2023 128/155 (82,6%), 2024 147/155 (94,8%), 2025 132/154 (85,7%), 2026 85/125 (68%) — MELHORADO** (era 72/100/109/66). Investigado via `?diagnostico=nomes` (PR #140), achados 6 pares divergentes: "Atletico-MG"/"CA Mineiro", "Estudiantes L.P."/"Estudiantes de La Plata", "Liverpool Montevideo"/"Liverpool FC" (confirmado sem ambiguidade com o Liverpool inglês nas partidas da Libertadores), "Águilas Doradas"/"Rionegro Águilas", "Defensor Sporting"/"Defensor SC", "Talleres Cordoba"/"CA Talleres". De passagem, achado e corrigido no banco um **time duplicado real**: "Club Nacional" (Uruguai) tinha 2 registros (`teams.id` 513 e 535) com fixtures da Libertadores espalhadas entre os dois — consolidado no id 535, e o clube "CA Paranaense" (id 7) renomeado pro nome oficial atual "Club Athletico Paranaense" com alias pra grafia antiga da API. Restante em `sem_casamento` inclui "Nacional Asuncion" (time paraguaio que não existe no nosso banco — precisa ser importado, não é bug de nome) e outros casos ainda não investigados um a um.
- **Championship: 2023/2024/2025 500/557 cada (89,8%) — RESOLVIDO.** Era o pior caso proporcionalmente (204/128/118 de 557); investigado via `?diagnostico=nomes`, achado que 100% dos `sem_casamento` eram "Queens Park Rangers FC"/"West Bromwich Albion FC" (API usa "QPR"/"West Brom", sem token em comum) e depois "Sheffield United FC" (API usa "Sheffield Utd", "utd"≠"united"). Dois `ALIASES_MANUAIS` resolveram as três temporadas de uma vez. Os 17 restantes em cada temporada NÃO são mais problema de nome (`?diagnostico=nomes` confirma que todos os 24 nomes de clube batem) — plateau consistente nas 3 temporadas sugere fixtures específicas sem corners/xg disponíveis na API-Football mesmo, não bug de matching.
- Primeira Liga: 2023 86/306, 2024 85/306, 2025 85/306.
- Copa do Brasil: 2022 5/122, 2023 47/122 (chegou no limite de rodadas, não em "sem progresso" — pode ter mais margem), 2024 66/122.
- Brasileirão: **não tem esse dado na API-Football** (achado confirmado antes) — não faz parte desse backfill.

**Achado sobre taxa de casamento baixa**: o casamento de partida (`sync-match-stats.js`) é por DATA + nome de time (token-subset). Times com grafia/transliteração divergente entre nosso banco e a API-Football (clubes sul-americanos com acentos/abreviação diferente, seleções como "Turkey"/"Türkiye" ou "United States"/"USA", clubes recém-promovidos a competições europeias, ou apelidos sem raiz comum tipo "QPR"/"West Brom"/"Sheffield Utd' — já resolvido pro Championship) ficam em `sem_casamento` pra sempre (a partida nunca fica "completa" já que corners/xg continuam NULL) — isso NÃO corrompe dado (o matching seguro por token já garante isso), só deixa essas partidas específicas sem estatística. **Confirmado: rodar de novo não ajuda quando o teto é nome** — as mesmas partidas continuam em `sem_casamento` até um alias manual ser cadastrado. **Próximo passo, se quiser mais cobertura nas outras competições**: mesma técnica aplicada no Championship — `?diagnostico=nomes` por liga/temporada, revisar nomes divergentes um a um, cadastrar alias manual em `ALIASES_MANUAIS` quando fizer sentido (times sul-americanos em Libertadores são o próximo candidato mais provável, dado o padrão de acento/abreviação). Não é urgente — cobertura já é substancial em 6 das 7 competições e o Championship, o pior caso, já foi resolvido.

Outras pendências menores (não urgentes):
- Widget de odds (`WidgetOdds` em `AnaliseHistorica.jsx`) já está pronto pra mostrar Bet365/Betano assim que a extração acima funcionar — nada a mudar nele.
- Cron `odds-todas` (`vercel.json`, a cada 3 dias) já está ativo — vale monitorar o consumo real de cota nas próximas semanas e ajustar a frequência se necessário.
- Curva de movimento de linha (múltiplos snapshots de `odds_market` ao longo do tempo) já está sendo guardada (`captured_at`), mas ainda não tem visualização (gráfico) — só o snapshot mais recente aparece no widget hoje.
- **Segurança — chave `service_role` do Supabase hardcoded em texto puro, commitada no git (adiado a pedido do usuário, projeto particular sem pretensão de tornar público)**: 9 scripts em `arquivos_do_claude/` (`ingestao_stats_fbref.py`, `backfill_xg_understat.py`, `ingestao_historico_ligas.py`, `ingestao_football_data_org.py`, `modelo_stats_esperadas.py`, `backfill_xg.py`, `ingestao_odds_footballdata.py`, `modelo_dixon_coles_walkforward.py`, `modelo_dixon_coles.py`) têm `SUPABASE_KEY = os.environ.get("SUPABASE_KEY", "eyJ...")` com a chave `service_role` (acesso total, ignora RLS) como valor padrão. O `iat` do JWT bate exatamente com o da chave `anon` atualmente ativa (confirmado via `get_publishable_keys`) — indício forte de que é a chave `service_role` **em uso agora**, não uma já rotacionada. Commitado desde `244530e`. Quando for retomar: (1) rotacionar a `service_role key` no painel do Supabase, (2) remover o fallback hardcoded dos 9 scripts (exigir env var, sem default), (3) não vale reescrever histórico do git só por isso (repo privado).
- **RESOLVIDO — API-Football**: conta antiga estava suspensa (painel mostrava "Your account is Suspended"; erro "Missing application key" nos endpoints era só sintoma). Usuário criou conta nova (`headneurotico@gmail.com`, plano Free, 100 req/dia) e trocou `API_FOOTBALL_KEY` no Vercel — endpoints (`api/leagues-search.js`, `api/team-stats.js`) voltaram a funcionar. **Restrição nova descoberta**: essa conta free só libera temporadas **2022-2024** (bloqueia a atual, 2025/2026) — oposto do problema da football-data.org, que bloqueia temporadas antigas. Ver achado da Copa do Brasil abaixo.
- **CONCLUÍDO — população de cidade/estádio/país dos clubes (`?tarefa=info-clubes`)**: `teams.city`/`teams.stadium` (novas colunas) + `teams.country` (já existia, sem uso) populados via API-Football `/teams?id=X`, só pros 146 times que já têm crosswalk confirmado em `team_source_ids` (`source='api_football'`) — sem matching por nome, zero risco de colisão. Rodado em lotes de 20 (`?limite=N`) com pausa de 700ms entre chamadas — mesmo assim a conta free tem limite POR MINUTO (~10 chamadas) além do limite diário (100/dia); ao bater o limite por minuto a tarefa para o lote sem marcar nada como "sem dado" (campo fica NULL, tenta de novo sozinho no próximo run). **Status final: 145/146 preenchidos** (1 time sem dado de venue disponível na API-Football mesmo — fica pendente indefinidamente, não é bug). Os 365 clubes SEM crosswalk (a maioria) ainda não têm plano de população — resolver por busca de nome é mais arriscado (mesma classe de bug corrigida acima) e fica pra decisão futura.

## Arquitetura
- **quant-futebol-dados** (Supabase, projeto `cgurxgfdmpmsnrshqycx`, região sa-east-1): banco de dados e pipelines de ingestão
- **quant-predictor** (Vercel + GitHub `JBatistaCosta/quant-predictor`, projeto `prj_7fSfP9zv4i55F6qyWUvcv9SJueOk`): aplicação de predição
- **Front-end também publicável no Firebase Hosting** (projeto `agilsgh-65463878-f64e7`) **como alternativa ao domínio Vercel** — só o front (`dist/`) migra; as 12 funções `api/*.js` e os 3 crons continuam exclusivamente no Vercel (Firebase Hosting não roda esse código). `npm run deploy:firebase` builda com `.env.firebase` (fixa `VITE_API_BASE_URL=https://quant-predictor.vercel.app`, usado por `src/utils/apiUrl.js` pra toda chamada `fetch` virar URL absoluta) e publica via Firebase CLI. CORS liberado em `api/_lib/cors.js` (compartilhado por todos os endpoints, mesma convenção de `_lib/negbin.js`) pros domínios `*.web.app`/`*.firebaseapp.com` do Firebase — domínio customizado novo precisa ser adicionado em `CORS_EXTRA_ORIGINS` (env var no Vercel). Ver seção 3 do `README.md` pro passo a passo. Vercel continua sendo o destino padrão (opção do passo 2 do README) — o Firebase é uma segunda opção, não substitui nada.

## Schema do banco (tabelas principais)
- `leagues`, `teams`, `matches`, `match_stats`, `match_events`, `odds_market`, `model_predictions`
- `team_strengths`, `league_model_params` — pesos treinados persistidos (ataque/defesa por time, mando/rho por liga)
- `odds_market.snapshot` — distingue `pre_closing` (capturado dias antes) de `closing` (fechamento real, coluna "C" do football-data.co.uk)
- View `v_market_edge` — compara odds do modelo com mercado (já devigado), separado por `snapshot`
- **`leagues.territory_type`** — `'country'` / `'confederation'` / `'global'` / `'region'` / `'multi-region'` (adicionado ago/2026; todas as 16 ligas já populadas; suporta ingestão futura de competições regionais como Campeonato Paulista com código ISO 3166-2 BR-SP)
- **`leagues.territory_code`** — ISO 3166 Alpha-3 para ligas nacionais (BRA, ENG, DEU…), código de confederação para internacionais (UEFA, CONMEBOL, FIFA); Copa América corrigida para `type='international'`
- **`matches.match_stage`** — fase normalizada (7 buckets: `regular_season`, `group_stage`, `early_round`, `round_of_16`, `quarter_final`, `semi_final`, `final`, `playoff`); derivada de `matches.stage` bruta; índice `idx_matches_league_stage(league_id, match_stage)` para filtros no pipeline ML
- **`matches.is_neutral`** — boolean (default FALSE); a preencher na ingestão futura para palco neutro
- **`matches.aggregate_advantage`** — integer nullable: NULL = jogo único/grupos; positivo = mandante leva vantagem no agregado; negativo = visitante; 0 = agregado empatado (2ª mão)

## Modelo de produção
- **Dixon-Coles** (`modelo_dixon_coles.py`) — gols, mercados 1X2 e over/under 2.5
- Treino por liga (`TEMPORADAS_TREINO_POR_LIGA`): Brasileirão = 2023-2024; as 5 europeias (PL/PD/SA/BL1/FL1) = 2022-2024 (2022 melhorou 3 de 5 ligas, validado com log loss real em 2025)
- Teste sempre em 2025
- Decaimento temporal: XI=0.0018 (meia-vida ~13 meses) — **fixo, nunca foi calibrado por validação cruzada de verdade**, é um chute inicial razoável
- `modelo_dixon_coles_walkforward.py` — versão com retreino periódico dentro de 2025. Resultado vs. estático: **empate técnico** (BSA/PD/SA melhoram, PL/BL1/FL1 pioram) — sem vencedor claro, não decidido qual usar em produção
- `modelo_stats_esperadas.py` — GLM Poisson pra xG/chutes/chutes-no-gol/escanteios, deriva mercados extras do mesmo jeito (over/under por estatística)

## Achados importantes (testados, não repetir investigação)
1. **Mando por time**: testado com 6 temporadas reais (2019-2024, 5 ligas europeias) — **descartado**. Heterogeneidade que parecia real com pouco dado (Ligue 1 se destacando) era majoritariamente ruído de amostra pequena; com mais dado, virou uma das ligas MAIS homogêneas. Mando único por liga é adequado. Detalhes em `ideias_futuras.md`.
2. **Filtro bayesiano dinâmico (EKF) pra força dos times**: tentado, **não passou em validação** — bug de deriva de gauge parcialmente corrigido, mas erro residual não resolvido (provavelmente precisa de IEKF, linearização iterada). Pausado, não descartado — candidato a retomar comparando com walk-forward.
3. **Backtest de edge vs. mercado real (1X2, O/U gols)**: **sem evidência de vantagem** contra Bet365, Pinnacle ou média de mercado — nem em odds pré-fechamento nem em fechamento real. Brier Score do modelo é sistematicamente pior que o do mercado (diferença pequena mas consistente, ~0,005-0,009). Teste de Closing Line Value (correlação entre edge do modelo e movimento de linha) também negativo — modelo não antecipa movimento de mercado. Relatório completo em `relatorio_backtest_edge.md`.
4. **Escanteios — Brier Score revela overconfidence**: em 2 das 5 ligas (Bundesliga, La Liga) o Brier é PIOR que "chutar 50% sempre" (>0,25). Diagnóstico: calibração boa no meio da distribuição (35-55% previsto), mas excesso de confiança nas pontas (73% previsto → só 52,5% real, com 80 jogos de amostra, não é ruído).
5. **Causa raiz do overconfidence em escanteios — CONFIRMADA, NÃO IMPLEMENTADA AINDA**: escanteios são superdispersos (variância/média = 1,60-1,76 em todas as ligas) enquanto gols não são (1,04-1,15, Poisson serve bem). O modelo de escanteios usa Poisson (mesma lógica do Dixon-Coles aplicada por estatística, sem covariância entre elas) — **deveria ser binomial negativa**. NÃO IMPLEMENTADO ainda, só diagnosticado.
6. **Log-loss confirma overconfidence nos mercados binários (O/U gols e escanteios) — calibração com Platt Scaling implementada**: medido em cima de `model_predictions` (split temporal 70/30, treino=passado/teste=futuro). Achado central: nos mercados binários, o modelo bruto é **PIOR que a "climatologia"** (chutar sempre a frequência histórica) — `dixon_coles_v1` O/U2.5: log-loss 0,698 vs 0,692 da climatologia; `dixon_coles_walkforward_v1` O/U2.5: 0,687 vs 0,692 (esse já vence, por pouco); `stats_glm_v1` escanteios O/U9.5: 0,693 vs 0,693 (empate técnico). Já o mercado 1X2 (3 classes) tem edge real: log-loss 1,003 vs 1,064 da climatologia baseada na frequência marginal (home 45,5%/draw 24,7%/away 29,8%). Platt scaling (`y ~ sigmoid(a·logit(p)+b)`, ajustado só no split de treino) melhorou log-loss no teste em todos os 3 casos binários testados (+0,4% a +1,7%) — coeficiente `a` sempre < 1 (0,34–0,45), confirmando que o modelo precisa comprimir a probabilidade em direção a 50% (está sistematicamente confiante demais). Isotonic Regression teve resultado misto (ajudou em 2 casos, piorou em 1) — com ~1.300-1.500 amostras de treino é mais propensa a overfit que o Platt (só 2 parâmetros). Coeficientes salvos em `model_calibration` (tabela nova), prontos pra consumir tanto daqui quanto do pipeline Python — **calibração ainda não está sendo aplicada em nenhuma predição em produção**, só calculada e persistida.
7. **BUG CORRIGIDO — `disp_r` da Binomial Negativa de escanteios calibrado errado, causando overestimação**: o método original (mean²/(variância-mean) sobre a distribuição AGREGADA de toda a liga) misturava variação ENTRE jogos (times diferentes → λ diferente, já capturado pelo modelo) com a variação DENTRO de um jogo — inflava a dispersão assumida e, consequentemente, as probabilidades de "over" (NB tem cauda mais gorda que Poisson pro mesmo λ; se a dispersão real é menor do que a assumida, o modelo superestima sistematicamente). Corrigido usando resíduo de Pearson condicionado no λ de cada partida (`alpha = Σ((real-λ)²-λ)/Σλ²`, `r=1/alpha` — estimador padrão de dispersão NB2). Valores antigos vs. novos (`league_model_params`, stat='corners', param_name='disp_r'): Premier League 88,2→**165,5**; La Liga 62,1→**41,0**; Serie A 40,1→**67,3**; Bundesliga 68,1→**30,0**; Ligue 1 57,9→**25,4** (fallback pra ligas sem calibração: 63,3→**65,85**). Direção do erro não era uniforme entre ligas (PL/Serie A estavam SUBestimando r, ou seja, SUPERestimando dispersão/over; Bundesliga/Ligue 1 o oposto) — por isso "superestimando muito" não aparecia igual em toda liga. Validado: com `r` corrigido, NB bate Poisson em 18 de 20 combinações liga×linha testadas (8.5/9.5/10.5/11.5), contra resultado bem mais fraco/inconsistente do `r` antigo.

## PRÓXIMOS PASSOS PENDENTES (ordem sugerida)
1. ~~**Trocar Poisson por binomial negativa no modelo de escanteios**~~ — **feito no lado do produto** (não no `modelo_stats_esperadas.py` em si, que continua Poisson): `api/corners-model.js` (Vercel) trata o TOTAL de escanteios do jogo como uma única Binomial Negativa, com `disp_r` calibrado por liga via método dos momentos direto em `match_stats` (16k+ linhas já no Supabase — não precisou esperar o script Python rodar de novo). Parâmetros salvos em `league_model_params` (stat='corners', param_name='disp_r'): Premier League 88.2, La Liga 62.1, Serie A (Itália) 40.1, Bundesliga 68.1, Ligue 1 57.9 — fallback 63.3 (média) pra ligas sem dado calibrado (Brasileirão, Champions, Eurocopa). `AnaliseEvento.jsx` chama esse endpoint automaticamente ao trocar os times e pré-popula `cornersModel='negbin'` + `cornersDisp`. Ainda vale rodar a validação com dado sintético e migrar o próprio GLM pra NB quando o pipeline Python for retreinado — o que está em produção agora é um fit direto na variância real, não uma reestimação do modelo GLM inteiro.
2. **Testar pareamento de escanteios com chutes/xG como covariável** — hoje cada estatística (xg, shots, shots_on_target, corners) é modelada isolada; escanteios têm ligação mecânica com chutes bloqueados e pressão ofensiva que está sendo ignorada.
3. Recalibrar XI (decaimento temporal) por validação cruzada temporal — nunca foi feito, valor atual é chute.
4. Decidir entre modelo estático vs. walk-forward por liga (resultado empatado, não decidido).
5. Considerar fonte paga (TheStatsAPI) ou OddsPapi (250 créditos/mês, só viável como coleta gradual daqui pra frente) se quiser odds reais de escanteios.
6. Fadiga/dias de descanso: precisa de nova tabela de fixtures (todas competições, não só liga doméstica) — não iniciado, só documentado.

## Scripts existentes (pasta local do usuário)
`ingestao_api_football.py`, `ingestao_football_data_org.py`, `ingestao_stats_fbref.py`, `backfill_xg_understat.py`, `ingestao_odds_footballdata.py`, `ingestao_historico_ligas.py` (2019-2022, 5 ligas europeias), `ingestao_escanteios_footballdata.py` (backfill de escanteios/chutes/cartões via football-data.co.uk), `ingestao_odds_fechamento.py` (odds de fechamento real, colunas "C"), `modelo_dixon_coles.py`, `modelo_dixon_coles_walkforward.py`, `modelo_stats_esperadas.py`.

## Documentação existente
`modelo_predicao_documentacao.md`, `ideias_futuras.md` (mando por time descartado, fadiga documentada, escanteios com achados parciais), `relatorio_backtest_edge.md`, `forca_dinamica_desenho.md` (filtro bayesiano pausado).

## Front-end (quant-predictor) — dois schemas de "times" coexistindo no mesmo banco
- `equipes`/`instituicoes` (RLS autenticado, CRUD manual pela tela Times.jsx) — cadastro feito à mão pelo usuário, 48 registros.
- `teams`/`leagues`/`matches`/`team_strengths` (RLS leitura pública, escrita só via service_role do pipeline Python) — 221 times, 14k+ partidas reais ingeridas.
- Essas duas tabelas NÃO tinham nenhuma referência cruzada. Adicionada coluna `equipes.pipeline_team_id` (FK pra `teams.id`, nula até vincular manualmente) — a tela Times.jsx agora tem um botão "Vincular" por equipe que busca em `teams` e grava o vínculo; `TimeDetalhe.jsx` mostra `team_strengths` (ataque/defesa treinados) e médias reais de `match_stats` quando o vínculo existe. `team_strengths` está **vazia** até o pipeline Python persistir os pesos treinados lá — até isso acontecer, a seção mostra "ainda não treinado".
- **RLS estava desabilitado em 11 tabelas do pipeline** (`teams`, `leagues`, `matches`, `match_stats`, `odds_market`, `model_predictions`, `model_stat_estimates`, `team_strengths`, `league_model_params`, `match_events`, `team_source_ids`) — corrigido: RLS habilitado + policy de leitura pública (dado público de futebol, sem PII; escrita continua exclusiva do service_role key usado pelos scripts Python).
- **`equipes.pipeline_team_id` foi populado em massa**: as ~13 seleções cujo nome batia exato com `teams.name` foram vinculadas automaticamente; os 196 clubes de `teams` (`is_national_team=false`) ganharam uma equipe/instituição nova cada (tipo='clube'), já vinculada — total foi de 48 pra 244 equipes, 209 com vínculo. Registrado em `importacoes` pra rastreabilidade.
- **`ligas` ganhou 8 linhas espelhadas de `leagues`** (Premier League, La Liga, Serie A, Bundesliga, Ligue 1, Brasileirão, Champions League, Eurocopa), com `ligas.external_id` (nova coluna) guardando o código da football-data.org (`PL`, `BSA` etc) pra re-sincronizar sem duplicar depois.
- **Casamento de times na importação de jogos (`ImportarJogos.jsx`/`api/fixtures.js`) agora tenta por ID externo antes de por nome**: `teams.external_id` é o ID numérico da football-data.org (confirmado batendo com o ID real de times como Palmeiras=1769) — quando a fonte usada é football-data.org, `api/fixtures.js` devolve esse ID em cada jogo (`mandante_external_id`/`visitante_external_id`) e o front cruza com `teams.external_id` → `equipes.pipeline_team_id` antes de cair no casamento por nome de string (frágil: "Brazil" da API não batia com "Brasil" cadastrado). IDs da API-Football (fonte 1, tentada primeiro) continuam sem ponte salva — `team_source_ids` só tem IDs do fbref, não da API-Football — então esses jogos ainda caem no fallback por nome.
- **Escudos, rodada/fase e histórico/tabela de jogos**: `teams.crest_url` (link, não a imagem — padrão `https://crests.football-data.org/{external_id}.png`) preenchido pros 197/221 times com `external_id`; mesmo padrão em `ligas.simbolo_url`. `matches.round`/`matches.stage` (colunas novas) preenchidas via backfill pontual na football-data.org usando a extensão `http` do Postgres direto em SQL (evita transportar ~7k linhas de dado pela conversa) — só cobre 2023-2025 pras 5 ligas europeias grandes + Brasileirão + Champions + Eurocopa, porque o plano grátis retorna 403 pra temporadas mais antigas (mesma restrição documentada em `api/fixtures.js`). `TimeDetalhe.jsx` ganhou histórico paginado de `matches`; `LigaDetalhe.jsx` (nova página, `/ligas/:id`) mostra os jogos de uma liga vinculada ao pipeline com seletor de temporada, separados por fase/rodada (cai em agrupamento por mês quando não há rodada real) e paginados em blocos de rodada.
- **`api/sync-matches.js`** (novo, roda via Vercel Cron todo dia às 06:00 UTC — `vercel.json`): mantém `matches` com a temporada ATUAL de cada uma das 8 ligas do pipeline, descobrindo a temporada perguntando `currentSeason` pra própria football-data.org (evita erro de calendário — Brasileirão é ano civil, europeias são ago-mai). Faz upsert por `external_id`, então cobre tanto atualização de placar/status de jogos já disputados quanto jogos futuros ainda não jogados (`status='scheduled'`, sem placar) — é isso que faz partida futura aparecer em `LigaDetalhe.jsx`/`TimeDetalhe.jsx`. Cria times novos automaticamente (upsert por `external_id`) quando encontra um time promovido/novo que ainda não está em `teams` (testado: Remo e Chapecoense, promovidos ao Brasileirão 2026, não estavam na base). **Precisa da variável de ambiente `SUPABASE_SERVICE_ROLE_KEY` no Vercel** (diferente da `SUPABASE_KEY` normal) — já configurada, testado em produção: 1.957 jogos futuros inseridos de uma vez (PL/PD/SA/BL1/FL1 2026 inteiras, ainda não começaram; BSA 2026 com 202 jogos futuros + 177 já disputados).
- **`api/sync-match-stats.js`** (novo, chamado manualmente por enquanto — não está no cron): a football-data.org NÃO tem chutes/escanteios/cartões/xG no plano grátis (só placar) — esse endpoint busca isso na API-Football pros jogos que `sync-matches.js` trouxe. Sem ponte de ID salva entre as duas fontes, o casamento é por DATA + nome do time aproximado (fuzzy). Processa em lotes pequenos (`?limite=N`, padrão 20) porque gasta 2 chamadas de API por partida — rodar tudo de uma vez em ligas com temporada inteira facilmente estoura cota do plano grátis da API-Football.
- **Painel de estatísticas dos modelos** (`api/model-stats.js` + página `/modelos`, `ModelosStats.jsx`): Brier Score, log-loss, acurácia (taxa de acerto do favorito do modelo, por partida) e comparação com a odd de fechamento média do mercado (devigada — `odds_market`, `bookmaker='media_mercado'`, `snapshot='closing'`) e curva de calibração por quintil, agrupado por modelo+mercado+liga. `odds_market` usa exatamente a mesma nomenclatura de `market`/`selection` de `model_predictions` (`1X2`/`over_under_2.5`, `home`/`draw`/`away`/`over`/`under`) — casamento direto, sem fuzzy matching. Escanteios não têm odds no banco (só existe histórico de odds de gols via football-data.co.uk), então nesse mercado o painel mostra só a métrica do modelo. Botão de exportar gera relatório em Markdown a partir do que estiver filtrado na tela.
- **Métricas pré-jogo disponíveis hoje**: `match_stats` guarda por time/partida chutes, chutes no alvo, posse, escanteios, faltas, cartões (quase todos os 16k jogos) e xG/PPDA/expected_points/np_xG/deep_completions (só ~10,5k, via Understat). NÃO existem (nem coluna nem dado): xA, passes por terço, esquema tático/escalação, impedimentos. Elo hoje só existe estático e hardcoded pra seleções (`src/data/selecoes.js`); `team_strengths` (ataque/defesa por clube) está desenhada mas vazia — nenhum script grava nela ainda.
- **`team_elo_external` (novo) + `api/sync-clubelo.js`**: primeiro passo pro Elo interno por clube/liga que ficou combinado — guarda o HISTÓRICO COMPLETO de rating do ClubElo.com (fonte pública, sem chave, desde décadas atrás pros clubes grandes) por clube, não só o valor atual; serve tanto como referência externa (pra suprir desatualização do Elo interno que ainda vamos construir) quanto como semente inicial do Elo geral (cross-league), proporcional à força média de cada liga. Casamento de nome é heurístico (sobreposição de palavras significativas após tirar sufixos tipo "FC"/"CF"/"AFC" e nossos nomes não terem `country` preenchido pra cruzar) — times sem correspondência confiável (score < 0.5) ficam de fora, reportados em `sem_correspondencia` na resposta, pra reconciliação manual depois. Roda em lotes (`?limite=N`, padrão 15, já que cada time síncrono = 1 chamada HTTP trazendo milhares de linhas de histórico) e só processa quem ainda não tem linha salva, a menos que `forcar=true`; também aceita `?time_id=X` (um time só, sempre força) e `?time_id=X&nome_clubelo=Nome` (pula o casamento automático, usa o nome exato — pra reconciliação manual). Taxa de casamento automático observada no lote inicial: ~60-70% (times menores/com nome muito diferente do ClubElo ficam de fora). **`Times.jsx` ganhou UI pra isso**: badge "ClubElo vinculado" ou botão "Buscar ClubElo" por linha (só aparece pra times já vinculados ao pipeline, já que a chave é `teams.id`), com campo de nome manual quando a busca automática falha; filtro "Vinculados sem ClubElo" na barra de busca (usa a view nova `vw_team_elo_status`, mais leve que consultar `team_elo_external` direto — essa tem uma linha por período de rating, não por time). **Importante: ClubElo NÃO cobre clubes brasileiros** (testado: `Palmeiras`/`Flamengo` não retornam nada, zero linhas com `country=BRA` no snapshot inteiro) — só clubes europeus (e mais alguns fora da Europa, mas não Brasil). Isso significa que a semente do Elo geral só funciona pras 5 ligas europeias; o Brasileirão nasce "a frio" (1500 flat) e converge só com confronto direto ao longo do tempo mesmo.
- **`team_elo`/`team_elo_history` (novo) + `api/model-maintenance.js` (`?tarefa=elo`)**: o Elo interno de fato, prometido no achado anterior. Dois escopos: `liga` (um rating por liga doméstica — 1,4,7,10,13,16 — recalculado sobre TODAS as partidas finalizadas daquela liga em ordem cronológica, todo time começa em 1500, sem depender de nada externo) e `geral` (cross-liga — só se ajusta em partidas da Champions League, liga 19, semeado com a média do rating `team_elo_external` quando existe, senão 1500). Fórmula: Elo clássico com multiplicador de diferença de gols (`G=1` se diff≤1, `1.5` se diff=2, `(11+diff)/8` se diff≥3 — mesmo espírito do World Football Elo Ratings de seleções), K=20, vantagem de casa=65 pontos. Testado em produção: recompute processando as 6 ligas + o geral tudo numa chamada só deu timeout (504, "Task timed out after 60 seconds") em 2 de 3 tentativas — mesmo com `vercel.json` já com `functions.maxDuration=60`. **Corrigido rodando em lotes por escopo**: `?tarefa=elo&liga_id=X` recalcula só uma liga doméstica (~1/6 do total de partidas), `?tarefa=elo&escopo=geral` recalcula só o cross-liga (só partidas de Champions League, bem menor) — cada chamada apaga e regrava só a fatia correspondente (`.match()` no delete, não mais `DELETE` da tabela inteira), continua idempotente. Precisa de 7 chamadas pra recompute completo (uma por liga + uma pro geral), igual ao padrão já usado em `sync-clubelo.js`/`sync-match-stats.js`. Validado em produção antes da otimização: Barcelona/Real Madrid/Man City com Elo de liga >1700 (times fortes), Palmeiras/Flamengo também altos no Brasileirão; escopo geral dos times brasileiros ficou em 1500/0 partidas como esperado (sem Libertadores no pipeline ainda).
- **Calibração Platt Scaling + Isotonic Regression no painel de modelos** (`api/model-maintenance.js` (`?tarefa=calibracao`) + aplicação em `api/model-stats.js` + tabela nova em `ModelosStats.jsx`): a rodada anterior de calibração (achado acima sobre log-loss) só cobria 3 combos model+market com Platt. Essa reescreve pra cobrir TODAS as seleções de todos os mercados — inclusive 1X2, calibrado por seleção (one-vs-rest: home/draw/away cada um como problema binário independente) — e adiciona Isotonic Regression (Pool Adjacent Violators Algorithm, puro JS, guardado como pontos `(x,y)` em `isotonic_x`/`isotonic_y` jsonb pra interpolação linear na hora de aplicar, já que não dá pra resumir em 2 coeficientes como o Platt). `model_calibration` ganhou coluna `selection` e constraint única `(model_name, market, selection, method)` — as 3 linhas antigas sem `selection` foram apagadas (redundantes com as novas, mais granulares). `/api/model-stats` agora devolve `log_loss_platt`/`brier_platt`/`accuracy_platt` e as variantes `_isotonic` ao lado das métricas cruas, e a página `/modelos` mostra uma tabela "Sem ajuste / Platt / Isotonic" por grupo (também no relatório exportado) — decisão de aplicar em produção continua manual, isso só ajuda a decidir.
- **Análise do relatório de model-stats (16/07)**: em TODO grupo com odds no banco, o log-loss/Brier do modelo cru perde pro mercado — nenhum modelo bate o mercado em qualidade de probabilidade hoje. Edges positivos isolados (ex.: Visitante +2.5pp no 1X2, Over +2.7pp no O/U 2.5, ambos dixon_coles_v1 Premier League) aparecem só numa seleção dentro de um modelo pior que o mercado no agregado — padrão clássico de ruído/overfitting, não de mercado ineficiente. `dixon_coles_walkforward_v1` é a única exceção real: bate o mercado em acurácia bruta no O/U 2.5 (58.0% vs 57.9%), mesmo perdendo em log-loss (acerta mais o lado certo, mas com probabilidade mal calibrada).
- **Gap de dado real encontrado nessa investigação**: `matches` está completo 2019-2025/26 nas 5 ligas europeias (sem gap, ao contrário do que parecia) — quem tem histórico curto é o Brasileirão (só 2023 em diante). O gap que IMPORTA de verdade: `odds_market` (closing/media_mercado) e `model_predictions` só existem pra **season 2025**, em TODAS as 6 ligas — não há odds nem previsões geradas pra 2019-2024 nem pro Brasileirão fora de 2025. É por isso que os grupos do relatório têm só ~300-380 jogos (uma temporada), não histórico multi-temporada — os scripts Python de modelo/odds só rodaram uma vez, pra season mais recente. Isso também limita o backtest de apostas abaixo: só dá pra simular contra dado real na season 2025.
- **`api/backtest-betting.js` (novo) + seção "Backtest de apostas simuladas" em `ModelosStats.jsx`**: responde à pergunta "log-loss melhor que o mercado prova EV+ de verdade?" — não prova, por isso esse endpoint simula apostas reais. Pra cada seleção com `p_modelo - p_mercado_devigado` acima de um limiar (`edge_minimo`, padrão 2pp), aposta na ODD REAL de fechamento (não na devigada — o vig é o que se paga de fato) em ordem cronológica. Duas formas de banca: flat (1 unidade fixa) ou Kelly fracionário (fração do critério de Kelly sobre a probabilidade usada pra apostar, capado em 25% da banca por aposta, sem compor — mantém grupos comparáveis). Aceita `usar_calibracao=platt|isotonic` pra apostar com a prob. calibrada em vez da crua. **O número que importa**: cada grupo sai com um intervalo de confiança de 95% do ROI via bootstrap (2000 reamostragens com reposição das apostas individuais) — só conta como "EV+ estatisticamente sustentado" (`significativo: true`) quando o limite INFERIOR do IC fica acima de zero, não só o ROI médio (que com ~300 jogos por liga facilmente é ruído de amostra pequena). Grupos ordenados por `roi_ic95_inferior` desc — esse é o critério real pra "escolher o melhor modelo", não o edge médio bruto do painel principal.
- **Vercel Hobby limita 12 Serverless Functions por deployment**: o deploy do backtest deu erro (`exceeded_serverless_functions_per_deployment`, "No more than 12 Serverless Functions can be added to a Deployment on the Hobby plan") — 13º arquivo em `api/*.js` estourou o limite. Corrigido fundindo `compute-elo.js` + `fit-calibration.js` (nenhum dos dois é chamado pelo frontend, só manualmente) num arquivo só: **`api/model-maintenance.js`**, com dispatch por `?tarefa=elo` (aceita `&liga_id=X` ou `&escopo=geral`, mesmo comportamento de antes) ou `?tarefa=calibracao`. Se o projeto crescer pra mais outro endpoint novo no futuro, esse é o primeiro lugar pra checar (`ls api/*.js | wc -l`, tem que ficar ≤12) antes de criar arquivo novo — considerar fundir com algo existente ou upgradar pra Pro.
- **Bug real em `LigaDetalhe.jsx`: temporadas mais novas não apareciam no seletor** — usuário reportou "não aparecem as ligas mais novas" no painel de ligas (só nas páginas de time). Causa: a busca de temporadas (`supabase.from('matches').select('season').eq('league_id', ...)`) não paginava — mesma classe de bug do corte de 1000 linhas do PostgREST já corrigida antes em `model-stats.js`/`sync-clubelo.js`. Sem `ORDER BY`, as primeiras 1000 linhas devolvidas tendem a ser as mais ANTIGAS (ordem de inserção), então pra ligas com 8 temporadas (~2900+ jogos) o `Set` de temporadas só via 2019-2022ish, nunca 2023+. `TimeDetalhe.jsx` não tinha esse bug (busca o histórico do time direto, paginado). Corrigido paginando de verdade só a coluna `season` (leve) até cobrir todos os jogos da liga.
- **`Eventos` virou a linha do tempo de TODOS os jogos reais** (`EventosLista.jsx` era um esqueleto vazio até agora, nunca tinha sido implementado — não foi substituição de nada funcional): `TimelineDatas.jsx` (novo componente reutilizável) mostra uma barra horizontal de 7 dias em volta da data selecionada (padrão hoje), com setas de avançar/recuar 1 dia, clique direto em qualquer dia da janela, atalho "voltar pra hoje" e um `<input type="date">` pra pular direto. Jogos do dia agrupados por liga; caixa lateral "Próximos jogos" com os 8 próximos agendados (clicar pula a data pra lá). `matches` não tem campo de campo-neutro — só dá pra indicar mandante/visitante (mandante sempre à esquerda), documentado como limitação. O cadastro manual antigo (`eventos`/`EventoNovo.jsx`) continua existindo em `/eventos/novo`, sem mudança — só a LISTA que virou a timeline de jogos reais.
- **`LigaDetalhe.jsx` ganhou classificação em destaque**: abas "Classificação" (padrão) / "Jogos" por temporada. Classificação é calculada em cima dos jogos já carregados pra temporada selecionada (sem consulta extra) — pontos corridos padrão (V=3/E=1/D=0), desempate por saldo de gols e depois gols pró; **não trata desempate por confronto direto** (regra oficial de algumas ligas), simplificação aceita. Linhas de jogo na aba "Jogos" agora linkam pra `/historico/:id`.
- **`AnaliseHistorica.jsx` (novo, rota `/historico/:matchId`)**: painel leve de forma recente + confronto direto, estilo sofascore/365score — **não roda o modelo preditivo** (isso continua exclusivo de `AnaliseEvento.jsx`, são páginas propositalmente separadas). Seletor de últimos 5/10/20 jogos (qualquer competição, não só a liga do confronto); pra cada time mostra sequência V/E/D, médias de gols pró/contra, e médias de escanteios/chutes/cartões quando `match_stats` tem dado. Seção de confronto direto (H2H) busca `matches` com `.or('and(home=A,away=B),and(home=B,away=A)')` — sintaxe PostgREST validada direto contra produção via curl (retornou os 5 últimos Real Madrid×Barcelona corretos) antes de subir. Linkado a partir de cada linha de jogo em `EventosLista.jsx` e `LigaDetalhe.jsx` (aba Jogos).
- **"Últimos N jogos" em `AnaliseHistorica.jsx` é sempre CRONOLÓGICO (por `match_date`), nunca por número de rodada** — decisão deliberada: jogos adiados/antecipados fazem a rodada nominal divergir da ordem real em que os jogos aconteceram, então ordenar por data é o único jeito de garantir que "últimos 5" sejam de fato os 5 mais recentes. A classificação em `LigaDetalhe.jsx` também não depende de rodada (soma todos os `finished` da temporada), então adiantamentos/atrasos não afetam pontuação/posição.
- **Recusado: capturar o widget de odds do Oddspedia (`widgets.oddspedia.com`)** — usuário perguntou se dava pra "captar os pacotes" (interceptar as chamadas de rede internas do widget) pra extrair dado estruturado. Recusado: isso seria engenharia reversa do feed proprietário de um provedor terceiro pra uso não autorizado (o widget é distribuído só pra exibição visual embutida, não como API pública — tentativa de acesso direto à página já retornou 403). Alternativa legítima pro objetivo de guardar/analisar odds: OddsPapi (ver achado abaixo), que tem termos que permitem consumo programático de verdade.
- **Integração OddsPapi (odds de Pinnacle/Bet365/Betano) — EM ANDAMENTO, só a fase de descoberta feita**: usuário já tem `ODDSPAPI_KEY` configurada no Vercel. Cota é só 250 requisições/mês no plano grátis (confirmado via doc oficial, `oddspapi.io/en/docs`), então a implementação foi dividida em duas fases pra não arriscar queimar cota com tentativa e erro:
  - **Fase 1 (feita, com 2 bugs reais corrigidos rodando em produção): `api/model-maintenance.js` `?tarefa=odds-descobrir`** — 3 chamadas fixas (`/v4/tournaments?sportId=10`, `/v4/markets`, `/v4/bookmakers`), cacheadas em `oddspapi_cache` (tabela nova, chave/valor jsonb) pra nunca precisar rechamar; confere se os slugs esperados de pinnacle/bet365/betano existem na lista real de casas (todos batem: `pinnacle`, `bet365`, `betano`); faz uma 4ª chamada (`/v4/odds-by-tournaments`) só pra UM torneio já resolvido, devolvendo a amostra crua no JSON de resposta.
    - **Bug 1 — parâmetro de bookmaker**: `/v4/odds-by-tournaments` exige EXATAMENTE 1 bookmaker via `?bookmaker=` (singular) — a doc pública sugeria lista separada por vírgula (`bookmakers=a,b,c`), que retorna 400 "Invalid number of bookmakers specified". Fase 2 vai precisar de 1 chamada por casa (3 por liga, não 1).
    - **Bug 2 — heurística de nome não é confiável pra esse caso, mesmo com filtro por país**: tentativa 1 (só sobreposição de token) colidiu Brasileirão×Serie A Itália no mesmo torneio genérico "Serie A", e La Liga×"Copa de SM La Reina" (copa feminina) só pela palavra "la". Tentativa 2 (com filtro por `categoryName`=país + métrica Jaccard) ainda errou: Serie A Itália foi parar em "Coppa Italia Serie C" porque nosso label "(Itália)" batia token com "Italia" do nome do torneio errado, e Brasileirão/La Liga ficaram sem correspondência por divergência de grafia ("Brasileirão" vs "Brasileiro" na OddsPapi — tokens diferentes após normalizar). **Decisão final**: parou de confiar em heurística automática pra gravar o mapeamento — `liga_oddspapi_tournament` foi resolvido MANUALMENTE por inspeção direta do cache via SQL (`select ... where categoryName ilike 'brazil'/'england'/etc`) e está fixo: Brasileirão=325 ("Brasileiro Serie A"), Premier League=17, LaLiga=8, Serie A=23, Bundesliga=35, Ligue 1=34. A tarefa `odds-descobrir` agora só REPORTA sugestões da heurística (`sugestoes_heuristica`) pra ligas ainda sem mapeamento confirmado — nunca sobrescreve o que já está resolvido.
    - **Formato real da resposta de odds** (achado na amostra, diferente do que a doc paráfraseada sugeria): cada fixture tem `participant1Name`/`participant2Name` (nomes legíveis, ex. "Arsenal FC"/"Coventry City" — participant1 = mandante), `bookmakerOdds.{slug}.markets.{marketId}.outcomes.{outcomeId}.players["0"]` com `price` (decimal) E o campo `bookmakerOutcomeId` = `"home"`/`"draw"`/`"away"` diretamente (não precisa resolver outcomeId→nome via `/v4/markets`, o outcome já vem semântico). Mercado 1X2 = marketId `"101"` (outcomes 101/102/103 = home/draw/away) — ainda falta confirmar o marketId exato de Over/Under 2.5 gols na fase 2.
  - **Fase 2 (FEITA): `?tarefa=odds&liga_id=X` sincroniza de verdade**. Descoberta importante: `/v4/odds-by-tournaments` já devolve nome dos times + horário do jogo JUNTO com as odds (não precisou de chamada separada em `/v4/fixtures` como o plano original previa) — custo real é só **3 chamadas/liga** (uma por bookmaker), não 4. O mercado de Over/Under 2.5 tem VÁRIOS marketId diferentes (um por linha: 2.0, 2.25, 2.5, 2.75, 3.5...) todos com o mesmo `marketName` genérico ("Over Under Full Time") — a linha certa só aparece dentro do `bookmakerOutcomeId` de cada outcome, no formato `"2.5/over"`/`"2.5/under"` (descoberto inspecionando a amostra real salva em `oddspapi_cache`, sem gastar cota nova). 1X2 usa `marketName='Full Time Result'` com outcomes `"home"`/`"draw"`/`"away"` diretos. Casamento com `matches`: por data (tolerância 36h, cobre fuso) + nome de time fuzzy (`nomesBatem`, mesmo padrão do `sync-match-stats.js`) — só considera `matches` com `status='scheduled'`. **Janela adaptativa** (pedido do usuário — "se houver poucos jogos aumentar a base"): antes de gastar as 3 chamadas de uma liga, conta jogos agendados nos próximos 7 dias (consulta local, sem custo); se vier menos que 3, alarga pra 14 e depois 21 dias; se mesmo assim não achar jogo suficiente, PULA a liga sem gastar cota (`pulado: true` na resposta). Grava em `odds_market` (tabela já existente) com `snapshot='pre_closing'`, sempre um INSERT novo (nunca upsert) — cada sincronização é um ponto novo na curva de movimento de linha, usando `captured_at`.
  - **`?tarefa=odds-todas`**: roda a tarefa acima nas 6 ligas em sequência (pior caso 18 chamadas — 3×6 — mas normalmente bem menos, já que a janela adaptativa pula ligas sem jogo suficiente). Ligado ao cron em `vercel.json` (`0 8 */3 * *`, a cada 3 dias) — matemática de cota: ~10 rodadas/mês × até 18 chamadas = até ~180/mês, dentro do limite de 250 mas com pouca folga; se a temporada tiver jogo toda semana em todas as ligas o consumo real fica perto do teto. Ajustar a frequência em `vercel.json` se sobrar ou faltar cota.
  - **Widget de odds comparativas** (`AnaliseHistorica.jsx`, componente `WidgetOdds`): painel lateral (2ª coluna, ao lado do cabeçalho do confronto) mostrando a odd mais recente de cada casa por seleção, uma tabela por mercado (1X2 e O/U 2.5), com a melhor odd de cada linha destacada em verde. Busca só a captura mais recente por (casa, mercado, seleção) — a curva completa fica salva no banco mas não é exibida ainda (possível próximo passo: gráfico de movimento de linha).
  - **Testado em produção (16/07): rate limit real da OddsPapi entre chamadas sequenciais** — `?tarefa=odds&liga_id=1` (Brasileirão, único em temporada agora — as 5 ligas europeias estão em pré-temporada, `liga_id=4` testado primeiro só confirmou a janela adaptativa pulando por falta de jogo nos próximos 21 dias, sem gastar cota) rodou pinnacle com sucesso (4 jogos casados, 12 linhas de 1X2 inseridas — sem O/U 2.5 pra esses jogos, provavelmente a casa ainda não abriu essa linha, times costumam postar 1X2 primeiro) mas bet365 e betano vieram com HTTP 429 "rate limited" — a doc pública menciona "500ms de cooldown" mas 2 de 3 chamadas sequenciais imediatas já estouraram. Corrigido com 800ms de intervalo entre as chamadas de bookmaker dentro do mesmo `tarefaOddsSync`.

- **Copa Libertadores adicionada ao pipeline (2023-2026, 2022 indisponível) + tabela `liga_fonte_externa`**: `leagues`/`ligas` ganharam a Libertadores (`external_id='CLI'`, `league_id=23`) — confirmado via football-data.org que a competição está no plano grátis já em uso, mesmo shape de dado das outras. `api/model-maintenance.js` ganhou `?tarefa=backfill-competicao&codigo=X&temporada=AAAA` (genérica, não hardcoded pra Libertadores) reaproveitando a lógica de `sync-matches.js`, que só cobre a temporada atual. Backfill rodado: 2023/2024/2025 com 155 jogos cada (100% sincronizados), 2026 com 142/147 (5 falharam de propósito — confrontos futuros do mata-mata ainda sem chaveamento definido na API, times `null`); 95 times novos criados. **2022 bloqueado (403)** — plano grátis só libera as ~4 temporadas mais recentes por competição, sem fonte alternativa gratuita cotada ainda. `CLI` entrou em `LIGAS_PIPELINE` (`sync-matches.js`), então o cron diário mantém 2026 atualizada sozinho daqui pra frente. Elo interno e modelo Dixon-Coles **não** foram estendidos pra Libertadores nessa rodada — só ingestão de partidas/times.
  - **Nova tabela `liga_fonte_externa`** (league_id, sistema, identificador, cobertura, notas): mapeamento unificado dos IDs/códigos de cada liga em cada sistema externo usado no projeto, pra agilizar imports futuros sem precisar redescobrir toda vez. Populada só com o que já estava confirmado em código/tabelas existentes (nada adivinhado): `football_data_org` (9 competições, espelha `leagues.external_id`), `fbref` (6 — as 5 europeias + BSA, de `ingestao_stats_fbref.py`), `understat` (5 — só europeias, **não cobre Brasileirão/copas continentais**, de `backfill_xg_understat.py`), `football_data_co_uk` (5 — códigos tipo `E0`/`SP1`, CSVs históricos de odds/escanteios, **fonte diferente da football-data.org apesar do nome parecido**, de `ingestao_odds_footballdata.py`/`ingestao_escanteios_footballdata.py`), `oddspapi` (6, copiado de `liga_oddspapi_tournament`). **`api_football` ficou de fora de propósito** — a chave está inválida em produção agora (achado à parte, ver pendências), não dava pra confirmar IDs reais via `/leagues` sem adivinhar, e o projeto tem convenção explícita contra gravar mapeamento sem confirmação. Preencher quando a chave for trocada.
- **Script de descoberta de campeonatos (`arquivos_do_claude/descobrir_campeonatos_disponiveis.py`) + 4 competições novas importadas**: script roda offline, consulta football-data.org/API-Football/soccerdata (fbref/understat/football-data.co.uk/ClubElo) e compara com `leagues`/`liga_fonte_externa` pra reportar só o que é novo, sem gravar nada (decisão de importar continua manual). Primeira rodada achou 4 competições novas na football-data.org, todas cadastradas e com backfill completo: **Championship** (`ELC`, Inglaterra 2ª divisão — 2023/24/25 com 557 jogos cada, 2026 com 552, todas 100% sincronizadas), **Eredivisie** (`DED`, Holanda — 306 jogos/temporada, 2023-2026), **Primeira Liga** (`PPL`, Portugal — 306 jogos/temporada, 2023-2026), **Copa do Mundo FIFA** (`WC` — só 2026 acessível no plano grátis, 2018/2022 bloqueados; vinculada à entrada manual já existente em `ligas` id=3 via `external_id` em vez de duplicar; torneio em andamento, 104 jogos com 102 já finalizados). Total: 4.775 partidas novas. Todas as 4 entraram em `LIGAS_PIPELINE` (`sync-matches.js`).
  - **Bug real corrigido rodando em produção — timeout no backfill de competições grandes**: `tarefaBackfillCompeticao` fazia um `upsert` por partida sequencialmente — funcionou pra Libertadores (155 jogos/temporada) mas deu `FUNCTION_INVOCATION_TIMEOUT` no Championship (24 times, ~552-557 jogos/temporada, bem mais que os ~380 de uma liga de 20 times), processando só ~270 de 552 antes de estourar os 60s. Corrigido montando todas as linhas primeiro e fazendo upsert em lotes de 200 (reaproveitando `fatiar()`, já existente no arquivo pra outras tarefas) — mesma classe de otimização que pode valer a pena revisitar em outros loops de upsert-por-item do projeto se aparecer timeout de novo.
- **Copa do Brasil importada via API-Football (2022-2024) — primeira competição do pipeline vinda de uma fonte diferente da football-data.org**: confirmado que não existe na football-data.org (lista completa das 13 competições do plano grátis conferida, só europeias + Libertadores + Copa do Mundo). `league id 73` na API-Football, achado via `api/leagues-search.js` assim que a chave nova ficou ativa. Cadastrada em `leagues` (`id=28`, **sem** `external_id` — esse campo continua reservado só pro código da football-data.org, não faz sentido misturar namespaces de ID na mesma coluna) + `ligas` (tipo `copa_nacional`, novo valor de tipo) + `liga_fonte_externa` (`sistema='api_football'`, `identificador='73'`).
  - **Nova tarefa `?tarefa=backfill-api-football&api_football_id=X&temporada=AAAA`** (`api/model-maintenance.js`) — mesma ideia do `backfill-competicao`, mas resolve a liga via `liga_fonte_externa` em vez de `leagues.external_id`, e casa/cria times via `team_source_ids` (`source='api_football'`, mesmo crosswalk já usado pro fbref/understat) em vez de `teams.external_id` — os dois sistemas de ID (football-data.org e API-Football) não têm nenhuma relação entre si. Custo de API mínimo: 1 chamada por liga+temporada (`/fixtures` já traz os times junto, sem chamada extra por time).
  - **Backfill rodado**: 2022/2023/2024 com 122 jogos cada, 100% sincronizados (366 partidas novas) — 2025/2026 bloqueadas pela restrição de temporada da conta free (ver achado acima). 146 times novos/casados gravados em `team_source_ids`. **Não entrou em `LIGAS_PIPELINE`** (`sync-matches.js`) — a temporada atual está bloqueada nessa conta, não haveria o que sincronizar automaticamente; revisitar se a 2025/2026 ficar acessível (upgrade de plano ou mudança da API).
  - **Achado sobre xG/xGA/odds por liga (pergunta do usuário, respondida consultando o banco real)**: xG só existe via Understat, só pras 5 ligas europeias (PL/PD/SA/BL1/FL1) — Brasileirão e todas as copas continentais/internacionais têm ZERO linhas com `xg` preenchido, mesmo `ingestao_stats_fbref.py` tendo sido desenhado pra também cobrir o Brasileirão via FBref (não roda ou falha silenciosamente pra ele). xGA não é coluna própria (é o xG do adversário na mesma partida). Odds: as 5 europeias têm cobertura rica via football-data.co.uk (`ingestao_odds_footballdata.py`, 1.900-5.700 linhas/casa/liga, snapshots `pre_closing`+`closing`); Brasileirão só tem 24 linhas via OddsPapi (só Pinnacle); as outras 7 competições do pipeline (Libertadores, Champions, Eurocopa, Copa do Mundo, Championship, Eredivisie, Primeira Liga, Copa do Brasil) não têm odds nenhuma — calibração de verdade só é estatisticamente sólida nas 5 ligas europeias.
- **BUG CORRIGIDO — casamento de nome por substring bruta em `resolverOuCriarTimeApiFootball` corrompeu 7 times durante o import da Copa do Brasil**: `nomesBatemTime` normalizava removendo TODO espaço/pontuação e depois fazia `includes()` (substring) sem tamanho mínimo nem limite de palavra — siglas curtas de times brasileiros colidiram por acaso com substrings de nomes concatenados de times não relacionados. Achado ao investigar a observação do usuário de que Libertadores tem times de várias federações: auditoria (`team_source_ids` × `matches`) achou 9 vínculos errados afetando 7 `team_id`: **ABC**→colado em Atalanta BC (id 192), **ASA**→Galatasaray SK (367), **GAS**→Monagas SC (547), **Nova Venécia**→NEC (624), **Juazeirense**+**Azuriz**+**Amazonas**→AZ Alkmaar (627, os 3 no mesmo id), **Ypiranga-RS**→seleção do Irã (680, o mais grave: misturava jogo de seleção da Copa do Mundo de verdade com jogos de clube brasileiro), **Real Noroeste**→Oeste (715, que também tinha 1 vínculo correto). **Corrigido**: `normalizarNomeTime`/`nomesBatemTime` reescritos pra usar matching por TOKEN (conjunto de palavras, subconjunto/superconjunto) em vez de substring bruta — mesmo padrão já comprovado em `ingestao_stats_fbref.py` (`normalizar`/`match_times`). Dado corrompido removido: as 366 partidas da Copa do Brasil (`matches.external_id like 'af_%'`, único prefixo usado por essa fonte) e as 146 linhas de `team_source_ids` (`source='api_football'`) foram apagadas por completo — nenhum outro dado dos 7 times afetados foi tocado (Eredivisie do AZ, Copa do Mundo do Irã etc. continuam intactos). **Próximo passo**: re-rodar `?tarefa=backfill-api-football` pras 3 temporadas (2022/2023/2024) já com o matching corrigido, e reconferir com a mesma query de auditoria antes de considerar encerrado.
- **`api/sync-match-stats.js` corrigido (matching por token, "pendente" por campo, rate limit) + tentativa de backfill de escanteios/xG do Brasileirão — CONCLUSÃO: a API-Football NÃO TEM esse dado pra essa competição**: 3 bugs corrigidos nessa tarefa (mesmo padrão de substring→token do achado acima; "pendente" antes significava "sem nenhuma linha em match_stats", então pulava o Brasileirão inteiro por já ter chutes/posse/cartões de outra fonte; sem pacing/detecção de rate limit por minuto). Também corrigido `acharLigaNaApiFootball`: buscava a liga por NOME ("Brasileirão Série A", nosso nome interno) — a API-Football chama a mesma competição de "Serie A" com país "Brazil" (`league_id=71`, confirmado via `/api/leagues-search?nome=Serie A` e salvo em `liga_fonte_externa`), então a busca por nome sempre falhava ("liga não encontrada"). Depois de corrigido tudo isso e rodar de verdade contra a temporada 2024 (2025/2026 bloqueadas pela restrição de temporada do plano free, já documentada): **chutes vieram preenchidos normalmente, mas escanteios e xG vieram NULL em 100% das linhas testadas (8/8)** — a resposta de `/fixtures/statistics` da API-Football pra partidas do Brasileirão simplesmente não inclui os tipos "Corner Kicks"/"expected_goals" (diferente do que acontece com as 5 ligas europeias, onde o mesmo endpoint retorna esses campos). **Não é bug, é limitação real da fonte** — não vale insistir rodando `sync-match-stats` de novo pro Brasileirão via API-Football; qualquer nova tentativa só vai re-confirmar NULL e gastar cota à toa (essas partidas continuam marcadas como "pendentes" pra sempre, já que nunca vão ter `corners`/`xg` preenchidos por essa fonte). Se quiser escanteios/xG reais do Brasileirão no futuro, precisa de outra fonte (FBref já está mapeado em `liga_fonte_externa` pra isso mas não está rodando de fato — ver achado mais antigo sobre `ingestao_stats_fbref.py`; ou TheStatsAPI, se a chave trial voltar a funcionar — ver pendência da sessão sobre `THE_STATSAPI_KEY` retornando `KEY_REVOKED`).

- **Integração FotMob (xG detalhado + estatística por JOGADOR, nível de dado que não existia antes) — fase de validação concluída, só Brasileirão/temporada atual**: usuário trouxe sugestão do Google AI Overview (lib `fotmob-api` do PyPI) pra fechar o gap já documentado acima ("xG só existe via Understat, Brasileirão tem ZERO"). **Não é API pública/oficial** — são os mesmos endpoints internos que o site www.fotmob.com usa pra se renderizar, sem autenticação nem SLA, mesmo espírito de scraping já usado pra FBref/Understat neste projeto (diferente do caso do Oddspedia, recusado antes, que era um widget de produto comercial pago). **A lib `fotmob-api` do PyPI está desatualizada** (usa `/api/*`, retorna 404 em tudo) — o endpoint real hoje é `www.fotmob.com/api/data/*`, descoberto por tentativa direta.
  - **Schema novo**: `match_source_ids` (crosswalk match_id↔fonte externa, mesma ideia de `team_source_ids` mas pra partidas — reutilizável por futuras fontes), `match_stats_fotmob` (time/partida — xG aberto por tipo de jogada, xGOT, duelos, desarmes, ~30 métricas), `match_player_stats_fotmob` (JOGADOR/partida — rating, xG, xA, xGOT, chances criadas — **não existia NENHUM dado nesse nível antes**), `match_shots_fotmob` (chute a chute com xG individual e coordenadas x/y). Todas com RLS + policy de leitura pública, mesmo padrão das tabelas do pipeline.
  - **Crosswalk**: `team_source_ids` ganhou `source='fotmob'` pros 20 times do Brasileirão — casamento manual por identidade de clube (nomes batem 1:1, sem ambiguidade, não precisou de heurística automática).
  - **2 bugs de mapeamento de chave corrigidos ANTES de generalizar** (mesma disciplina de "descobrir 1-2 chamadas, inspecionar JSON real, só then escrever parser final" já usada com OddsPapi): (1) o TÍTULO em inglês de vários stats não bate com a chave interna real usada pra buscar o valor — ex. título "Duels won" tem chave real `duel_won` (singular), "Fouls committed"→`fouls`, "Tackles"→`matchstats.headers.tackles`, "xG open play"→`expected_goals_open_play` (não `xg_open_play` como seria intuitivo); descoberto comparando `key` vs `title` de cada stat no JSON bruto, não adivinhando. (2) `yellow_cards` existe em DOIS grupos diferentes do JSON (`top_stats` e `discipline`) com o mesmo nome de chave — o de `top_stats` vem sempre `null` (campo morto/legado), só o de `discipline` tem o valor real; sem o achado, 100% das linhas teriam cartão amarelo nulo silenciosamente. Ambos corrigidos e revalidados (0 nulos inesperados em nenhuma coluna após a correção, 364/364 linhas).
  - **Backfill rodado (temporada 2026 completa, 182 jogos finalizados até 18/07)**: 364 linhas de `match_stats_fotmob`, 8.330 de `match_player_stats_fotmob`, 4.857 de `match_shots_fotmob`, 182 de `match_source_ids` — 100% dos jogos casados com `matches` interno (crosswalk de time + tolerância de 36h na data, mesmo padrão já usado em `sync-match-stats.js`), zero falha de rede. Pacing de 1.3s entre chamadas (sem limite de cota documentado, só risco de bloqueio de IP — abordagem conservadora).
  - **Script reutilizável**: `arquivos_do_claude/ingestao_fotmob.py` — aceita `--liga-id`/`--fotmob-league-id`/`--temporada`/`--limite`/`--forcar`, idempotente via `match_source_ids` (pula jogos já sincronizados a menos que `--forcar`). **Não cria crosswalk de time automaticamente** (mesma disciplina do `liga_oddspapi_tournament`: mapeamento crítico não se resolve por heurística sem supervisão) — só lê o que já está confirmado em `team_source_ids`. Exige `SUPABASE_URL`/`SUPABASE_KEY` via variável de ambiente, **sem default hardcoded** (deliberado — não repetir o problema de segurança já documentado nos outros 9 scripts desta pasta).
- **Integração FotMob expandida pra 2023-2025 (Brasileirão completo, 4 temporadas) + `stats_raw` + resposta a 3 perguntas do usuário sobre o desenho**:
  - **Dado é sempre observado pós-jogo, nunca previsão**: as 4 tabelas `*_fotmob` só recebem `matchDetails` de jogos com `status.finished=true` — xG ali é a soma do valor esperado de cada chute que realmente aconteceu (shot-quality model do FotMob sobre eventos reais), nunca uma previsão pré-jogo. Isso nunca colide com `model_predictions`/`model_stat_estimates` (previsões do pipeline, tabelas totalmente separadas). Garantia é por lógica do script (só busca `finished`), não por constraint de banco — considerar trigger de validação se algum dia outro script escrever nessas tabelas.
  - **Fonte sempre identificada, nunca sobrescreve outra fonte**: tabelas com sufixo `_fotmob` nunca tocam `match_stats` (onde Understat/fbref já gravam `xg`/`xg_source` pras 5 ligas europeias) — cada fonte de xG fica isolada na sua própria tabela, comparação entre fontes é só um JOIN, nunca um upsert arriscado.
  - **Sempre tabela relacional tipada, nunca JSON como estrutura primária**: colunas tipadas continuam sendo a única coisa consultada no dia a dia. Adicionado (estava desenhado na migration original mas não populado — gap encontrado respondendo a essa pergunta): coluna `stats_raw` (jsonb) em `match_stats_fotmob`/`match_player_stats_fotmob` agora É preenchida com o bloco bruto completo, como rede de segurança contra erro de mapeamento de chave — sem custo de performance nas queries normais (ninguém precisa tocar nela pra uso comum). Retrofit aplicado nas 182 linhas da temporada 2026 que já tinham sido inseridas sem essa coluna (via cache local, zero chamada de rede nova).
  - **Crosswalk `team_source_ids` (`source='fotmob'`) expandido de 20 para 29 times**: Brasileirão troca ~5-9 times por temporada (acesso/rebaixamento) — 2023-2025 trouxeram 9 times que não estavam no elenco de 2026 (AC Goianiense, América-MG, Ceará, Criciúma, Cuiabá, Juventude, Fortaleza, Goiás, Sport Recife), mapeados manualmente por identidade de clube, sem ambiguidade.
  - **Bug real corrigido rodando em produção — crash em jogo com `playerStats`/`shotmap` null explícito**: `.get(chave, {})` do Python só protege contra CHAVE ausente, não contra a chave existir com valor `None` — um jogo real do Brasileirão 2023 tem esses dois campos `null` no JSON do FotMob (dado incompleto na própria fonte), derrubando o backfill inteiro no meio da temporada 2023 (240/380 processados, nada persistido — só salvava no fim, todo o progresso foi perdido). Corrigido com `(x.get(chave) or {})` em vez de `.get(chave, {})`, e adicionado checkpoint incremental (grava a cada 40 partidas) + try/except por partida no script de produção — mesma disciplina de "sync roda em lotes, uma falha não derruba o resto" já usada em `sync-match-stats.js`/`sync-clubelo.js`.
  - **Backfill final (4 temporadas completas, 2023-2026)**: 1.322 `match_source_ids`, 2.642 `match_stats_fotmob` (2023/2024: 380 jogos cada; 2025: 379/380, 1 jogo sem stats na própria fonte, não é bug; 2026: 182 finalizados até 18/07), 60.299 `match_player_stats_fotmob`, 35.313 `match_shots_fotmob`. Zero falha de rede, zero jogo sem casar com `matches` interno.
  - **Ainda pendente**: (1) expandir crosswalk + backfill pras 5 ligas europeias (já têm xG via Understat — decidir se vale ter as duas fontes ou se FotMob fica só pra ligas sem Understat); (2) decidir se/como esses dados alimentam o modelo de produção — hoje são tabelas novas isoladas, `match_stats.xg`/`corners` (usado pelo Dixon-Coles/NB de escanteios) não foi tocado; xGOT por jogador é um sinal forte pra "qualidade de finalização" que não existe em nenhum lugar do pipeline hoje.
- **Tabela `players` (nova) — dimensão de jogador, não existia NADA disso antes**: usuário perguntou "o banco já tem jogadores?" — resposta era não, só `fotmob_player_id`+nome espalhados em `match_player_stats_fotmob`/`match_shots_fotmob`, sem tabela própria. Criada `players` (nome, idade, país, valor de mercado, posição — enum cru do FotMob, sem decodificar por falta de legenda confiável na resposta — foto, último time/partida visto, `raw_lineup` jsonb de backup). É SNAPSHOT via upsert (idade/valor de mercado da última vez visto em campo), não histórico. **Foto do jogador**: `photo_url` construído deterministicamente (`images.fotmob.com/image_resources/playerimages/{fotmob_player_id}.png`, testado e confirmado HTTP 200/imagem real, sem custo de chamada — funciona só com o ID que já se tem). **Bandeira do país**: testadas 5 variações de URL do CDN do FotMob pra bandeira, todas 403 — não inventada nenhuma URL de terceiro; só `country_code` (ISO) fica salvo, renderização de bandeira fica por conta do frontend. População inicial: 1.765 jogadores (nome+foto+último time, zero chamada nova, só reaproveitando `match_player_stats_fotmob` já existente) + 436 enriquecidos com idade/país/valor de mercado via lineup dos 12 jogos mais recentes do Brasileirão 2026 (cobre os 20 times atuais pelo menos 1x). `ingestao_fotmob.py` atualizado pra popular/atualizar `players` automaticamente em todo sync futuro (extrai `lineup`, que já vem de graça em cada `matchDetails`).
  - **Bug real corrigido — `fotmob_player_id="0"` é placeholder, não identificador único**: usuário perguntou "existe id único que facilite identificação?" — resposta é sim (`fotmob_player_id` é estável por PESSOA, não por vínculo com time; confirmado empiricamente: jogadores como Luan Cândido/Caio Paulista/Lucas Barbosa mantêm o mesmo id atravessando transferências entre até 4 clubes diferentes no período 2023-2026), **exceto pelo id `"0"`**, que o FotMob usa como placeholder pra jogador sem perfil vinculado no sistema deles (visto em jovens/estreantes — `match_player_stats_fotmob` tem 85 linhas assim, **13 pessoas distintas** compartilhando o mesmo "id" falso). Como a tabela `players` faz upsert por `fotmob_player_id`, isso estava silenciosamente misturando 13 pessoas num único registro (o último sincronizado sobrescrevia os anteriores). Corrigido: `ingestao_fotmob.py` pula esse id ao montar a dimensão de jogador (a linha de estatística em `match_player_stats_fotmob` continua normal, só a tabela `players` não recebe esse caso) + `check (fotmob_player_id <> '0')` adicionado na tabela como segunda camada de proteção contra reintrodução futura. Registro corrompido já inserido foi apagado.
  - **FK `player_id` adicionada em `match_player_stats_fotmob`/`match_shots_fotmob` → `players.id`**: usuário perguntou "temos um id próprio do nosso banco?" — resposta é sim (`players.id`, mesmo padrão de `teams.id`/`matches.id`), mas até então nada referenciava esse id de verdade, as tabelas de stats só guardavam `fotmob_player_id` (texto) solto, sem FK. Adicionada coluna `player_id bigint references players(id)` (nullable) + índice nas duas tabelas, backfill via JOIN por `fotmob_player_id`: **99,86%** de cobertura em `match_player_stats_fotmob` (60.214/60.299) e **99,80%** em `match_shots_fotmob` (35.241/35.313). Os poucos sem correspondência são: placeholders de identidade não resolvida (achado outro além do `"0"` — **`"-1"`** também é placeholder, mesmo padrão, mesma correção aplicada e constraint estendida pra `not in ('0','-1')`) e um punhado de casos reais (~7 linhas, ex. jogador "Wallyson") que aparecem no mapa de chutes mas nunca em nenhum lineup capturado — não é bug, é lacuna de cobertura da própria fonte. `ingestao_fotmob.py` reordenado pra processar a dimensão `players` ANTES das tabelas de stats (precisa do id recém-upsertado pra já gravar a FK na mesma passada, sem query extra).
- **FotMob expandido pras 5 ligas europeias + Copa Libertadores (temporada atual, 2025/2026 e 2026 respectivamente) + catálogo de IDs em `liga_fonte_externa`**:
  - **IDs do FotMob descobertos via `/api/data/allLeagues`** (endpoint próprio que lista ligas por categoria — popular/international/countries), não adivinhados: Premier League=47, La Liga=87, Serie A=55, Bundesliga=54, Ligue 1=53, Libertadores=45 (achado dentro da categoria "international", busca por nome). Todos registrados em `liga_fonte_externa` (`sistema='fotmob'`), incluindo retroativamente o Brasileirão (`268`) que já estava em uso mas nunca tinha sido catalogado ali.
  - **Crosswalk de 143 times novos** (`team_source_ids`, `source='fotmob'`) casado por overlap de token normalizado (mesmo algoritmo já validado no projeto) só DENTRO do escopo de cada competição — importante porque existem times homônimos em competições diferentes (ex.: "Liverpool FC" inglês na Premier League E um "Liverpool FC" uruguaio na Libertadores; casar globalmente teria colidido os dois). 7 ambíguos e 7 sem candidato (apelidos como "Wolves"/"Inter"/"PSG"/"M'gladbach") resolvidos manualmente — 3 deles (Man United, Barcelona, Paris FC) por um erro real: o `fotmob_id` tinha sido **inventado de memória** em vez de extraído do JSON bruto, corrigido antes de inserir qualquer coisa. 2 casos de "Universidad Catolica" (Chile vs Equador, ambos times reais e distintos na base) e "Nacional" (Uruguai vs Bolívia) resolvidos consultando `/api/data/teams?id=X` da própria fonte (país confirmado, não chutado).
  - **Backfill (temporada atual, ~1.878 jogos)**: 3.748 `match_stats_fotmob`, 140.200 `match_player_stats_fotmob`, 82.646 `match_shots_fotmob`, 4.649 jogadores novos em `players`. 3 falhas de rede transitórias (connection reset), recuperadas manualmente depois — 1.878/1.878 jogos casados no final. FK `player_id` populada com 99,9%+ de cobertura nas linhas novas.
  - **Catálogo API-Football também completado pras 5 europeias** (pendência antiga documentada — "api_football ficou de fora de propósito" — resolvida): Premier League=39, La Liga=140, Serie A=135, Bundesliga=78, Ligue 1=61, confirmados chamando o endpoint de produção (`/api/leagues-search?nome=X`, que roda com a chave nova válida do Vercel — a sessão não tem a chave em texto puro, só o script já implantado). Endpoint ganhou o campo `id` na resposta (antes só devolvia nome/país/tipo, sem o identificador numérico — impossível catalogar sem ele).
  - **Sul-Americana, Mundial de Clubes FIFA e Intercontinental ficaram de fora dessa rodada** (pedido explícito do usuário) — não têm NENHUM dado no banco (nem liga, nem time, nem partida), diferente das 6 competições acima que já tinham base via football-data.org. Precisam de backfill completo de matches/teams antes de cogitar FotMob.
- **Frontend: painel `/jogadores` (paginado) + abas com dado FotMob em `/historico/:matchId` + `/jogadores/:id` (tendência de forma) — todos consumindo só o que já estava no banco**: `AnaliseHistorica.jsx` ganhou 3 abas ("Visão Geral"/"Estatísticas do Jogo"/"Jogadores") — as 2 novas só disparam `select` no Supabase na primeira vez que são abertas (nunca no load da página), pra não gastar consulta à toa em partida que o usuário nem olha. `Jogadores.jsx` (lista paginada, busca, ordenação) e `JogadorDetalhe.jsx` (`/jogadores/:id`, novo) são consumo puro de `players`/`match_player_stats_fotmob` já existentes — zero chamada nova ao FotMob. `JogadorDetalhe` responde à pergunta "dá pra quantificar desempenho ao longo do tempo?" com o que já tinha: nota/gols/assistências/xG/xA por partida (gráfico de barras coloridas por faixa de nota + tabela), sem precisar do próximo achado abaixo.
  - **Achado (não implementado, decisão consciente do usuário de adiar)**: heatmap de jogador (`coordinates` de toque em campo) e "traits" (radar de percentil comparando o jogador com outros da mesma posição, ex. Haaland 0.99 em gols/0.93 em finalizações/0.17 em ações defensivas — calculado pelo próprio FotMob) EXISTEM na fonte, achados em `/api/data/playerData?id={fotmob_player_id}` (endpoint POR JOGADOR, diferente do `matchDetails` já usado) — campos `firstSeasonStats.heatmap.coordinates` e `traits`. Esse mesmo endpoint também tem `marketValues.values` (série temporal de valor de mercado) e `statSeasons` (índice temporada×competição, com `entryId` pra puxar stats agregadas por temporada — não testado se dá pra buscar via outro endpoint ainda). Custo: **1 chamada por jogador**, não por partida — pros 6.100+ já cadastrados seria ~2h+ de pacing conservador, ordem de grandeza maior que qualquer backfill já feito. Usuário optou por NÃO importar agora e focar no que já dava pra fazer sem chamada nova (`JogadorDetalhe.jsx` acima). Se retomar: validar primeiro com um grupo pequeno (ex. elenco atual do Brasileirão, ~500-600 jogadores) antes de generalizar pros 6 mil.

- **Rodada de otimização pra apostas (18/07, PRs #70-#75, todos mesclados e validados em produção)**:
  - **Calibração Platt/Isotonic APLICADA em produção** (#70) — `model_calibration` deixou de ser só diagnóstico: `AnaliseEstatisticaJogo.jsx` (painel Modelo vs. Mercado) agora mostra a probabilidade calibrada e calcula o edge sobre ela (crua vira referência secundária). `src/utils/calibration.js` escolhe por combinação (model, market, selection) o método com MENOR log-loss medido no teste (`log_loss_calibrado` já salvo) em vez de assumir Platt sempre — validado com dado real que a escolha varia (ex.: walkforward/away vence com Isotonic).
  - **Faixas de probabilidade de gols e escanteios** (#71): bandas 0-1/2-3/4-5/6+ gols (mesma grade Poisson×Dixon-Coles do 1X2, só reagrupada) e ≤8/9-10/11-12/13+ escanteios (mesma NB do corners-model — `escanteios_esperados.total`+`disp_r` já vinham na resposta, zero chamada nova).
  - **`match_context_fotmob` — estádio (lat/long) + clima observado por partida** (#72): descoberto que o MESMO payload `matchDetails` já buscado pra stats traz `content.matchFacts.infoBox.Stadium` (nome/cidade/país/lat/long, praticamente toda partida desde 2023) e `content.weather` (temperatura/vento/umidade/precipitação/nuvens — **só temporada atual/mais recente de cada competição**, FotMob não guarda clima histórico; testado em 2023/2024 = ausente). Isso DISPENSOU expandir crosswalk de time (só 26% tinham cidade) e API de clima paga. Backfill completo: 3.198 partidas (3.197 com coordenadas, 2.225 com clima; 2 falhas 503 transitórias que o próximo sync recupera). `ingestao_fotmob.py` ganhou `parse_contexto_jogo` (grava em todo sync futuro). Painel "Estádio e clima" em `/estatisticas/:matchId`.
  - **Rating Elo-like por JOGADOR (`player_ratings`/`player_rating_history` + `?tarefa=player-elo`)** (#73/#74): ancorado na nota do FotMob (holística) + bônus/penalidade por gols, assistências, finalização (gols−xG) e criação (xA+chances). Global cross-competição, ACUMULATIVO com cursor (última linha do histórico = ponto de retomada; a 1ª versão carregava as 200k linhas inteiras por chamada e deu timeout — corrigido buscando stats só dos ids do lote via `.in`). Processamento inicial completo: ~3,2k partidas em 15 chamadas de 200, 4.518 jogadores com rating. Validação de cara: topo plausível (Arrascaeta 2134, Matheus Pereira 2057, Kane/Olise já altos com ~30 jogos), mediana 1501. **Viés conhecido: topo Brasil-pesado porque Brasileirão tem 4 temporadas de dado FotMob vs. 1 das europeias** (mais jogos = mais acúmulo). Card de rating em `/jogadores/:id`. Limitação documentada: partida antiga sincronizada depois do cursor não entra sozinha — reprocesso = `?tarefa=player-elo-reset` + re-rodar.
  - **`model_config` + painel de configuração de parâmetros** (#75, pedido explícito do usuário — ativar/desativar importância de parâmetro e comparar modelos): tabela genérica (config jsonb por model_name, escrita só service role), tarefas `config-get`/`config-set` (POST, merge parcial) / `player-elo-reset` em `api/model-maintenance.js`, seção colapsável em `/modelos` com toggle+peso por parâmetro do player_elo_v1, âncora FotMob ligável, K/nota neutra/minutos mínimos, salvar e resetar+reprocessar (explícito, com confirmação). Round-trip testado em produção. Mudança de config só vale dali pra frente — reprocesso é sempre explícito.
  - **Pendências que essa rodada cria**: (1) calibrar os pesos do player_elo contra resultado real (hoje é chute inicial documentado, mesmo status do XI); (2) agregar rating de jogadores escalados em score de ataque/defesa do TIME e ligar ao modelo de predição (era a parte 2 do pedido de scores — o rating individual é a fundação); (3) usar clima/estádio como feature de modelo (dado agora existe, nada o consome); (4) `ingestao_fotmob.py` deveria chamar `?tarefa=player-elo` (ou rodar a lógica) após cada sync pra manter o rating em dia — hoje é manual.

- **Rodada 2: expansão europeia + features de contexto pra gradient boosting (19/07, PRs #78-#87, todos mesclados e validados em produção)** — sequência de pedidos do usuário: expandir FotMob pras 5 europeias (5-8 temporadas) + features de fadiga/logística/ciclo de técnico/torneios de seleção pra CatBoost/XGBoost/LightGBM.
  - **Temporadas europeias 2019/20-2025/26 (7 temporadas, todas as viáveis — 8 não dava, nossa base de matches só começa em 2019)**: crosswalk `team_source_ids` (source='fotmob') expandido de 193 pra 210 times (46 novos: rebaixados/promovidos das 7 temporadas, cada um confirmado por matching de token + 1 caso resolvido manualmente checando a fonte, "Sheff Utd"→Sheffield United). Cobertura final: **~99% em todas as 35 combinações liga×temporada** (5 ligas × 7 temporadas) — só 2 falhas de rede pontuais (379/380) e um gap PRÉ-EXISTENTE de 101 jogos no Ligue 1 2019/20 (a própria tabela `matches` só tem 279/380 pra essa temporada, gap da fonte football-data.org, não do FotMob). Achado: FotMob tem xG/shotmap completos só a partir de 2020/21 — 2019/20 vem só com stats de time+notas (sem xG), documentado como limitação real, não bug.
  - **2 bugs reais de duplicidade corrigidos rodando o backfill em produção** (mesmo padrão "ON CONFLICT DO UPDATE command cannot affect row a second time" nos dois casos, causado por reconstruir o id a partir de `pdata["id"]`/chave do dict em vez de usar a chave já resolvida — o mesmo tipo de placeholder "0"/"-1" já documentado em `players`, só que sem filtro nesses dois pontos): (1) `ingestao_fotmob_elenco.py` no upsert de `team_transfers_fotmob`; (2) `ingestao_fotmob.py` no upsert de `match_player_stats_fotmob` (esse derrubava o script INTEIRO no meio de uma temporada — Premier League 2023/24 tinha parado em 22/380 antes da correção, recuperada depois rodando de novo isolada). Também corrigidos 2 cortes silenciosos de 1000 linhas do PostgREST em `ingestao_fotmob.py` (fetch de `matches` da liga e de `match_source_ids` sem `.range()` — mesma classe de bug já documentada várias vezes neste arquivo) que faziam temporadas novas caírem 100% em "sem par" no matching.
  - **`player_availability_fotmob` (desfalques) + `team_transfers_fotmob` (contratações)** — novo script `ingestao_fotmob_elenco.py`, 1 chamada por time (`/api/data/teams?id=X`, mesmo endpoint que já tinha `coachHistory`/`transfers`/`squad` descobertos por inspeção direta do JSON). Desfalques são SNAPSHOT do elenco atual (jogadores que saem do elenco são removidos antes do upsert, não ficam órfãos); transferências têm histórico preservado (upsert em chave natural). 210 times processados, 0 falhas na rodada final.
  - **`match_features_contexto` (features de contexto pra modelos de árvore) + `team_coach_history_fotmob`**: script novo `features_contexto.py`, rodado completo pra TODAS as ligas do pipeline (18.891 partidas alvo, 37.782 linhas partida×time). Cobertura: 98,6% com descanso real (`days_since_last_match`/`is_midweek_fatigue` <72h — limitação documentada: só conta competições que estão no NOSSO banco, Europa League e copas domésticas europeias não estão importadas), 70% com deslocamento real (`travel_distance_km`, haversine entre estádio do jogo anterior e o atual via `match_context_fotmob.stadium_lat/long` — cobertura = partidas com contexto FotMob sincronizado), 73% com técnico real (`manager_match_count`/`is_honeymoon_phase` ≤4 jogos, via `team_coach_history_fotmob` novo — granularidade temporada×técnico, vigência por partida É APROXIMADA pela ordem da lista, só jogos de liga). `key_players_missing_torneo`/`squad_natl_matches_window` são PROXY explícito (titulares por minutos de clube cujo país jogou Copa/Euro na janela de 30 dias — minutos reais de seleção não existem no banco, Copa Africana nem está importada) — só 14 linhas com desfalque de seleção detectado nessa rodada, esperado dado o recorte de torneios cobertos. Todo grupo de feature tem flag `*_filled` + valor imputado estável (7 dias, 30 jogos de técnico, 0km) pro gradient boosting não confundir ausência de dado com sinal real. Bug real corrigido no caminho: carregar `match_player_stats_fotmob` inteira (>250k linhas após a expansão) dava `statement timeout` do Postgres mesmo paginado — corrigido filtrando só os `match_id` relevantes (janela de 10 jogos anteriores de cada time-alvo) antes de buscar.
  - **Rating de jogador RESETADO E REPROCESSADO do zero** (`?tarefa=player-elo-reset` + ~70 chamadas de `?tarefa=player-elo&limite=200`) com o histórico europeu expandido — validação decisiva do que a rodada anterior já suspeitava (viés Brasil-pesado por diferença de cobertura de temporadas): **top 20 agora é 100% europeu** (Mbappé 3735, Kane 3463, Lewandowski 3367, Bruno Fernandes 3319, Salah 3186, Haaland 3125, Kimmich, Messi, De Bruyne...) — nenhum jogador brasileiro sobrou no top 20 (antes Arrascaeta liderava com 2134). Base cresceu de 4.518 pra **7.889 jogadores** com rating, espalhamento bem maior (1017-3735 vs 1365-2134 antes), mediana 1495.
  - **Importância da partida** (`AnaliseEstatisticaJogo.jsx`, não é feature do gradient boosting, é pro painel de UI): métrica PRÓPRIA (não existe em nenhuma fonte externa) — gap de pontos na classificação a título/vaga continental (~top 4)/rebaixamento (~3 últimos) × rodadas restantes, peso crescente no fim da temporada. Agregação por MÁXIMO dos dois times (não média) — decisão validada contra dado real: a média diluía um decisivo de rebaixamento (Ceará, última rodada Brasileirão 2025) contra adversário sem nada em jogo pra "importância média". Só se aplica a ligas de pontos corridos (≥8 times), não copas/mata-mata.
  - **Painel "Importância e desfalques"** em `AnaliseEstatisticaJogo.jsx`: desfalques só aparecem pra jogo AINDA NÃO DISPUTADO (o snapshot é do estado de hoje — mostrar lesionado atual num jogo de 2023 seria anacronismo).
  - **Pendências que essa rodada cria**: (1) as features de `match_features_contexto` ainda não alimentam nenhum modelo de predição — existem, mas nada as consome ainda (mesmo status de `match_context_fotmob`/clima da rodada anterior); (2) vigência de técnico por partida é aproximada (ordem da lista dentro da temporada, não data exata de troca) — se algum dia a data exata importar, precisaria de outra fonte; (3) proxy de fadiga internacional é fraco (só Copa do Mundo/Eurocopa cobertos, sem Copa Africana nem minutos reais de seleção) — descartar ou fortalecer esse proxy é decisão futura; (4) `features_contexto.py`/`ingestao_fotmob_elenco.py`/`ingestao_fotmob_coaches.py` não estão automatizados (rodados manualmente nesta sessão) — decidir se entram em rotina periódica.

- **Rodada 3: perfil avançado de jogador (19/07, PR #92 mesclado) — valor de mercado histórico, carreira, títulos, atributos**: pedido do usuário ("valor de mercado tem linha do tempo?" → "quanto mais informação melhor"). Fonte é OUTRO endpoint do FotMob, `/api/data/playerData?id=X` (por JOGADOR, diferente do `matchDetails` usado no resto do pipeline) — validado em 2 perfis bem diferentes (Mbappé e um jogador brasileiro pouco conhecido) antes de generalizar.
  - **4 tabelas novas**: `player_market_value_history` (série temporal completa — o pedido original, até 72 pontos/10+ anos no teste), `player_career_history_fotmob` (passagens por clube com jogos/gols/assistências por período), `player_trophies_fotmob`, `player_details_fotmob` (altura/pé/contrato/posições/traits — percentil vs. pares da mesma posição). Achado: já existia uma `player_market_value_history` VAZIA com schema mais simples, nunca documentada aqui (provavelmente de uma tentativa antiga não registrada) — recriada sem perda (0 linhas) com o schema completo.
  - **Duas formas de popular**: (1) `api/model-maintenance.js` `?tarefa=jogador-perfil&player_id=X` — sync sob demanda de 1 jogador, 1 chamada externa, rápido o bastante pro FRONTEND chamar direto (botão "Sincronizar" novo em `JogadorDetalhe.jsx`); (2) `arquivos_do_claude/ingestao_fotmob_perfil_jogador.py` (novo) — script standalone pro usuário rodar backfill em massa (~7.900 jogadores, ~3h20min no pacing conservador de 1,5s — bem mais caro que os scripts anteriores porque aqui é 1 chamada por JOGADOR, não por partida/time).
  - **`JogadorDetalhe.jsx`** ganha painel "Perfil avançado": linha do tempo de valor de mercado (SVG linha única, crosshair+tooltip no hover, seguindo a skill de dataviz do projeto), títulos, carreira, atributos físicos/contratuais.
  - **Heatmap e shotmap (mapa de toques / chute a chute) DELIBERADAMENTE FORA DE ESCOPO** — mesmo endpoint tem em `firstSeasonStats`, mas achado confirmado inspecionando o dado real (Mbappé): **"firstSeasonStats" NÃO é a carreira inteira**, é só a competição/temporada mais recente mostrada por padrão na página do jogador no FotMob (no teste, só ~41 chutes de ~1 mês — Copa do Mundo 2026, nem a temporada de clube dele). A carreira completa existe só como ÍNDICE em `statSeasons` (temporada×competição, cada combinação com um `entryId` próprio) — puxar heatmap/shotmap histórico de verdade exigiria descobrir um sub-endpoint por `entryId`, **não explorado ainda**. Se retomado no futuro: custo vira 1 chamada POR TEMPORADA POR JOGADOR (não 1 por jogador), ordem de grandeza maior que o já caro backfill deste perfil — validar com 1-2 chamadas de descoberta antes de generalizar (mesma disciplina de sempre).

- **Rodada 4: bug de reset incompleto de rating + painel de jogadores (19/07, PRs #95 e #96 mesclados) — pedido do usuário**: "incluir botão de resetar/recalcular rating e botão de importação em massa de dados dos jogadores (lotes de 20/20 ou 50/50 ou 100/100)... clube mostrado como vínculo na data atual... filtro de rating, além do valor de mercado."
  - **Bug real encontrado por acidente, corrigido antes de seguir (PR #95)**: `ConfigPlayerElo` (`ModelosStats.jsx`, criado no PR #75) tinha um loop com cap FIXO de 40 rodadas (200 partidas/rodada = 8.000 partidas) no botão "Resetar e reprocessar histórico" — correto quando a base era menor, mas depois da expansão europeia (~13.900+ partidas, Rodada 2) o cap parava o reprocesso pela metade **e ainda reportava "Reprocessamento concluído com a configuração atual."**, mascarando o problema. Descoberto validando a nova view (abaixo): `player_ratings` tinha caído de 7.889 (Rodada 2) pra 5.317, todas com timestamp dentro de uma janela de 13 minutos — sinal de um clique real nesse botão em produção que parou incompleto. Corrigido: loop sem cap arbitrário (sai quando a API confirma `partidas_restantes=0`), 500 rodadas só como rede de segurança contra erro real de API, mensagem de erro honesta se essa rede for atingida. Reprocesso completo disparado em produção depois do merge pra restaurar a cobertura (~68 rodadas).
  - **View `vw_jogadores_lista`** (migration `cria_view_jogadores_lista` + `add_perfil_sincronizado_vw_jogadores_lista`, `security_invoker=true`): resolve o **clube ATUAL na data de hoje** via LATERAL JOIN em `player_career_history_fotmob` (`start_date <= current_date <= end_date`, usando `team_source_ids` pra converter `team_fotmob_id`→`teams.id`) pra jogadores já sincronizados via perfil avançado (Rodada 3); cai pro snapshot antigo `players.last_team_id` quando ainda não sincronizado. Também expõe `rating`/`n_partidas` (join `player_ratings`) e `perfil_sincronizado` (boolean, existência em `player_details_fotmob`) — essa última coluna é o critério usado pela importação em massa pra saber quem ainda falta.
  - **`Jogadores.jsx`** (PR #96): troca a fonte de `players` (join estático) pra `vw_jogadores_lista`; adiciona rating como coluna/ordenação/filtro numérico (mínimo), e valor de mercado ganha filtro próprio (antes só existia como ordenação); botão "Resetar/recalcular rating" (mesma lógica corrigida do `ModelosStats.jsx`); botão "Importar em massa" com seletor de lote 20/50/100 — busca os próximos `perfil_sincronizado=false` priorizados por valor de mercado, chama `?tarefa=jogador-perfil` sequencialmente com pacing de 1,3s (mesmo padrão anti-bloqueio dos scripts Python).
  - **Testado localmente com dados reais de produção**, não só com o build passando: o app exige login (`ProtectedRoute`) e o sandbox bloqueia HTTPS direto do browser pro Supabase — contornado abrindo uma rota temporária sem proteção só pra teste (revertida antes do commit) e relayando as chamadas HTTPS via `context.route`+`fetch` do Node (que respeita `HTTPS_PROXY`). Confirmado por screenshot: lista com clube/rating/valor reais, ordenação por rating funcionando, filtro de rating mínimo reduzindo 11.438→1.581 jogadores corretamente. Os botões de reset/importação em massa não foram clicados nesse teste local (dependem do endpoint serverless, que só roda no preview/produção) nem no preview (protegido por Vercel SSO) — ficam validados pela reutilização do código já provado (mesma lógica de reset do `ModelosStats.jsx`, mesmo endpoint `jogador-perfil` já usado desde a Rodada 3).
  - **Pendência que essa rodada cria**: a importação em massa processa os jogadores sequencialmente NO BROWSER (não em background/servidor) — fechar a aba no meio de um lote de 100 interrompe a importação (idempotente, retomável clicando de novo, mas não sobrevive à navegação).

- **Rodada 5: importação de liga nova via FotMob, direto do frontend (19/07) — pedido do usuário**: "inclua um formulario no frontend (+Nova Liga) para importar ligas com base no fotmob (raspagem) incluindo o ano."
  - **Achado que mudou o desenho**: `arquivos_do_claude/ingestao_fotmob.py` (scripts existentes) só ENRIQUECE ligas que já têm partidas de OUTRA fonte (football-data.org ou API-Football) — casa fixtures do FotMob contra `matches` já existentes, não cria liga/time/partida do zero, e exige crosswalk de time resolvido manualmente antes de rodar (documentado no próprio script como decisão consciente, não automatizável sem supervisão). Pra "+Nova Liga" fazer sentido (liga que NUNCA existiu no banco), o caminho tinha que ser outro: criar liga+times+partidas diretamente do FotMob — sem a ambiguidade de casar nomes de fontes diferentes que motivava a cautela do script antigo (liga nova = times novos, sem outro sistema de ID pra confundir). Seguido o mesmo padrão já usado em `tarefaBackfillApiFootball` (Copa do Brasil): resolve time por nome contra os já existentes, cria se não achar.
  - **Gap real encontrado no meio do caminho**: `ligas` (cadastro manual, alimenta a lista em `/ligas`) só linkava com `leagues` (pipeline) via casamento de STRING `ligas.external_id = leagues.external_id` — funciona só pra ligas vindas da football-data.org. Ligas onboardadas via `backfill-api-football` (ex: Copa do Brasil) nunca ganhavam esse link nem apareciam com jogos em `/ligas/:id`, e às vezes nem tinham `ligas` cadastrada (só existiam em `leagues`, invisíveis na UI). Corrigido com uma coluna nova, `ligas.pipeline_league_id` (FK direta pra `leagues.id`, migration `add_pipeline_league_id_ligas`) — backfill automático pras 14 ligas antigas que já casavam por `external_id`, mais 1 vínculo manual confirmado (Copa do Brasil, único nome sem ambiguidade). `LigaDetalhe.jsx` passou a resolver por `pipeline_league_id` primeiro, com o casamento antigo por `external_id` só como fallback.
  - **2 tarefas novas em `api/model-maintenance.js`** (sem função serverless nova — Vercel Hobby já no limite de 12): `?tarefa=fotmob-liga-buscar&termo=X` (proxy de busca, `apigw.fotmob.com/searchapi/suggest` — domínio DIFERENTE do resto da raspagem FotMob, achado por inspeção; devolve nome/id/país, não escreve nada) e `?tarefa=backfill-fotmob-liga&fotmob_league_id=X&temporada=AAAA` (cria `leagues`+`liga_fonte_externa`(sistema='fotmob')+`ligas` na primeira chamada, depois só faz upsert de partidas — idempotente por `fotmob_league_id`, chamar de novo com outra temporada reaproveita a liga já criada em vez de duplicar). Temporada no formato livre do FotMob (ex: "2024" ou "2024/2025", documentado — não normalizado/validado, seguindo o mesmo formato que os scripts anteriores já usavam).
  - **`Ligas.jsx`**: novo botão "Importar do FotMob" ao lado do "+Nova Liga" existente (mantido intacto, cadastro 100% manual continua funcionando) — abre um painel próprio: busca por nome → escolhe o resultado → completa tipo/país/confederação/temporada → importa, mostra quantos jogos entraram e linka pra liga criada.
  - **Pendências que essa rodada cria**: (1) não há normalização de temporada — se o usuário digitar um formato que a liga em questão não aceita, o erro só aparece depois da chamada ao FotMob (mensagem genérica, não valida antes); (2) resolução de time por nome (`nomesBatemTime`) pode juntar clubes de países diferentes com nome parecido em ligas muito obscuras — mesmo risco já aceito em `backfill-api-football`, não é um risco novo introduzido aqui; (3) endpoint de busca (`apigw.fotmob.com`) é outro domínio não-oficial, sujeito a mudar sem aviso como o resto da raspagem FotMob já documentada.

- **Rodada 6: Model Benchmarking v4 (cartões/pendurados) + investigação de valor de mercado/estádio/outras fontes (20/07)** — pedido do usuário: levantar e importar dado novo pra virar feature, seguindo prioridade cartões > valor de mercado > estádio > outras fontes, sempre confirmando cobertura real antes de construir.
  - **Cartões por JOGADOR (prioridade 1, implementado) — `match_events` deixou de estar vazia**: confirmado por inspeção direta da API (não adivinhado) que `content.matchFacts.events.events` (MESMO payload matchDetails já usado pro resto do pipeline FotMob) tem a timeline completa de eventos, incluindo `type=="Card"` com `card` in `Yellow`/`Red`/`YellowRed` (segundo amarelo) por jogador. **Achado de fonte real, corrigido antes de generalizar**: `eventId` vem **0 em 100% dos eventos testados do Brasileirão** (placeholder, mesmo espírito do `fotmob_player_id` "0"/"-1" já documentado) — só as 5 ligas de elite europeias têm `eventId` real; corrigido gerando id sintético negativo por partida (estável entre re-syncs, já que a ordem de evento da API é determinística).
    - **`match_events` (schema original, existia desde sempre mas tinha 0 linhas) expandida** (migration `expand_match_events_cartoes`): + `source` (default 'fotmob'), `player_id` (FK `players.id`), `fotmob_event_id` (chave de idempotência, unique em `(match_id, fotmob_event_id)`).
    - **Script novo `arquivos_do_claude/ingestao_fotmob_cartoes.py`**: reaproveita o payload já usado por `ingestao_fotmob.py`, mas não precisa de crosswalk de time (usa `isHome` do evento + `home_team_id`/`away_team_id` da partida já casada em `match_source_ids`) — mais simples que os outros scripts FotMob. Idempotente (pula partida que já tem linha em `match_events`, `--forcar` reprocessa).
    - **Regra de "quantos cartões amarelos viram suspensão" pesquisada por competição (não adivinhada)**: Premier League (FA) 5/10/15 cumulativos (equivalente a limiar constante 5 com reset, já que é progressão aritmética de passo 5 — mesmo resultado de "quanto falta pro próximo limiar"); La Liga (RFEF) 5, reseta; Bundesliga (DFB) toda 5ª amarela; Ligue 1 (LFP) 5 a partir da 2025/26 (regra nova, substituiu "3 em janela de 10 jogos" — aplicada uniformemente à janela de treino inteira, simplificação aceita tipo `XI_DECAIMENTO`); Serie A (FIGC) limiar PROGRESSIVO `[5,5,4,3,2,1]` (1º ciclo em 5, reincidência em 5/4/3/2/1 depois sempre 1 — sistema de diffida); Brasileirão (CBF) 3, reseta. Tudo em `dados_historicos.CARTAO_LIMIAR_POR_LIGA`, configurável por liga (não hardcoded um valor só).
    - **`scripts/dados_historicos.py`**: `_carregar_cartoes_pre_jogo` (ponto-no-tempo, TREINO — calculado sobre o elenco que REALMENTE jogou cada partida via `match_player_stats_fotmob`, mesmo padrão de `_carregar_squad_rating_pre_jogo`; usa `merge_asof` com `allow_exact_matches=False` pra nunca vazar o cartão da PRÓPRIA partida) + `obter_cartoes_atuais` (AO VIVO — elenco regular atual, mesmo padrão de `obter_squad_rating_atual`). Features novas: `cartoes_acumulados_home`/`_away` (soma de cartões no ciclo atual do elenco) e `jogadores_pendurados_home`/`_away` (quantos jogadores estão a 1 cartão do limiar). `FEATURES_V4 = FEATURES_V3 + essas 4 colunas`.
    - **`modelos_ml.py`/`backtest_kelly.py`/`rodar_predicoes.py`/`ModelBenchmarking.jsx`**: `catboost_v4`/`xgboost_v4`/`lightgbm_v4` adicionados reaproveitando as MESMAS funções de treino/predição (só a lista de features muda, mesmo padrão de v2/v3) — `GRADE_HIPERPARAMETROS` do backtest generaliza automaticamente (loop `for _sufixo in (...)`), só precisou adicionar `"_v4"`. `montar_features_fixtures` (predição ao vivo) roda `obter_cartoes_atuais` pras 6 ligas do benchmarking (Brasileirão incluso, diferente de `squad_rating`/`liga` que só valem pro dataset europeu).
    - **Validado offline (fake Supabase client + dados sintéticos) antes de tocar produção**: regra de reset/limiar testada bit a bit (La Liga simples e Serie A progressiva, batendo com a pesquisa); ponto-no-tempo testado fim-a-fim (jogador com 4 cartões acumulados vira `pendurado=true` antes do 5º jogo, sem vazar o cartão do próprio jogo); os 3 modelos de árvore (catboost/xgboost/lightgbm) treinam e preveem com `FEATURES_V4` sem erro, tolerando NaN (mesmo padrão de elo/xG). `py_compile` + `npm run build` OK.
    - **Backfill histórico (~13.780 partidas, 6 ligas, ~6-7 temporadas) rodado via GitHub Actions dispatch** (`.github/workflows/cartoes_backfill.yml`, novo — `workflow_dispatch` com `liga_id`/`limite`/`forcar`, não entra em cron): volume total não cabe num job só dentro do teto de 6h do runner gratuito, then disparado 1x por liga (6 dispatches paralelos, cada um ~1,5-2h) em vez de 1 rodada sequencial das 6.
  - **Valor de mercado histórico (prioridade 2) — cobertura real confirmada BAIXA, PARADO PRA DECISÃO DO USUÁRIO em vez de forçar**: `players` tem 11.438 jogadores, só 174 (1,5%) com perfil sincronizado (`perfil_importado`) e 179 (1,6%) com alguma linha em `player_market_value_history` — resquício de teste de sessão anterior, como já documentado. Backfill completo precisaria de ~11.260 chamadas (1/jogador) no endpoint `playerData` do FotMob — o script já existe (`ingestao_fotmob_perfil_jogador.py`, Rodada 3), nunca foi rodado até o fim. Matemática de tempo: ~4-5h num job só (pacing 1,3-1,5s), CABE no teto de 6h do GitHub Actions, mas diferente de `match_stats_fotmob`/cartões (mesmo endpoint já testado em volume grande sem problema), esse volume nunca foi testado NESSA escala a partir de um runner de CI (IP compartilhado, não o IP "normal" de sessão usado nos backfills anteriores) — risco de bloqueio/rate-limit real, não documentado, e um bloqueio aqui poderia contaminar TODO o resto da raspagem FotMob do projeto (match_stats/cartões/contexto, todos o mesmo domínio). Por isso ficou como decisão explícita do usuário, não assumida.
  - **Coordenadas de estádio pra fixtures futuras (prioridade 3) — gap confirmado e função pronta, wiring completo fica pra depois**: só 8/3.139 partidas AGENDADAS tinham `stadium_lat`/`long` (via `match_features_contexto`, só cobre partida já finalizada). Confirmado direto no banco: times raramente trocam de estádio de casa (exceções reais existem — reforma, jogo fora de sede — mas são minoria clara nos dados). `dados_historicos.obter_estadio_provavel_mandante` (nova, testada contra produção via REST) usa o estádio de casa mais RECENTE já capturado em `match_context_fotmob` (qualquer status) como proxy — cobre 1.908/3.139 (60,8%) das agendadas hoje SEM nenhuma chamada de API nova. Wiring completo em `match_features_contexto` (INSERT novo pra partida agendada exigiria replicar o resto de `features_contexto.py` — fadiga/técnico/desfalque têm colunas NOT NULL) e como feature de modelo (`travel_distance_km` nunca entrou em nenhuma versão v1-v4 até agora) ficam documentados como próximo passo, fora do escopo desta rodada (cartões era o pedido principal).
  - **Outras fontes já parcialmente no banco, documentadas sem implementar (prioridade 4)**: **árbitro** (`match_context_fotmob.referee`) tem **99,6% de cobertura** (13.719/13.779) nas 6 ligas do benchmarking, MESMO payload já usado, zero custo — daria pra derivar "rigor do árbitro" (média de cartões/jogo apitados por ele, calculável em cima de `match_stats_fotmob` já existente) como feature futura, não implementado agora. **Clima** (`weather_temperature_c` etc.) continua em 15,5% (2.130/13.779) — mesma limitação já documentada (FotMob só guarda clima da temporada mais recente de cada competição), não vale a pena como feature de treino histórico.
  - **Backtest real de v4 rodado após a cobertura de cartões passar de 90% em todas as 6 ligas**: sem melhora consistente sobre v3 (log-loss/acurácia oscilam pra cima e pra baixo dependendo do modelo/mercado, dentro do que o projeto já trata como ruído) — único ponto de atenção real: `lightgbm_v4` no mercado 1X2 teve ROI simulado significativamente NEGATIVO (IC95% inteiramente abaixo de zero) no Test Set, não é prova de que a feature é ruim mas é sinal de cautela nesse modelo/mercado específico antes de usar pra apostar de verdade.
  - **Backfill de valor de mercado LIBERADO pelo usuário após teste pequeno (400 jogadores) confirmar sem rate-limit/bloqueio** — rodando em lotes sequenciais de 3.200 via `perfil_jogador_backfill.yml` (dispatch → aguarda terminar → dispatch do próximo, nunca em paralelo, pra não duplicar trabalho já que o script só marca "feito" no fim de cada jogador). **2 bugs reais corrigidos no meio do caminho** (achados rodando via GitHub Actions pela 1ª vez, nunca tinham aparecido rodando local): (1) `SUPABASE_URL`/`SUPABASE_KEY` sem `.strip()` em `ingestao_fotmob_cartoes.py`/`ingestao_fotmob_perfil_jogador.py` — secret do GitHub Actions colado com `\n` final quebra o parser de URL do httpx (mesmo bug já corrigido em `rodar_predicoes.py`, só que os 2 scripts novos/reaproveitados não tinham o fix); (2) upsert de `player_trophies_fotmob` sem dedup por chave de conflito derrubava o lote inteiro no primeiro jogador com troféus duplicados na mesma chave (mesma classe de bug já documentada pra `match_player_stats_fotmob`/`team_transfers_fotmob`) — corrigido com dedup + try/except por jogador.
- **Auditoria de nomenclatura de seleção (21/07, pedido do usuário) — 3 bugs reais de dado corrigidos em produção**:
  - **Chile e Venezuela coladas em clube homônimo**: nenhuma seleção "Chile"/"Venezuela" existia em `teams` — os jogos da Copa America 2024 dessas seleções estavam gravados sob `CF Universidad de Chile` (id 579) e `Universidad Central de Venezuela FC` (id 574), os CLUBES reais que também jogam Libertadores. Causa raiz confirmada consultando a API do FotMob direto: o `team_source_ids` (`source='fotmob'`) tinha o fotmob-id da SELEÇÃO (9762="Chile", 5800="Venezuela", confirmado via `/api/data/teams?id=X`, campo `country='INT'`) apontando pro `team_id` do CLUBE — resultado do resolvedor de time da sincronização da Copa America ter casado "Chile"/"Venezuela" (1 palavra) como subconjunto de token de "CF Universidad de **Chile**"/"Universidad Central de **Venezuela** FC" (variante nova do bug de token-matching já documentado — nome de 1 palavra vira subconjunto trivial de qualquer nome de clube que contenha essa palavra). Corrigido: criadas as seleções de verdade (`teams.id` 849="Chile", 850="Venezuela", `is_national_team=true`), realocados só os jogos/stats da Copa America 2024 (`matches`, `match_stats_fotmob`, `match_player_stats_fotmob`, `match_shots_fotmob`, `players.last_team_id` — escopado por `last_seen_match_id`, não por `team_id` cru, pra não mexer em jogador que já foi visto de novo pelo clube depois) pros IDs novos, crosswalk `team_source_ids` movido junto — os jogos de Libertadores dos clubes reais (2025/2026) ficaram intocados.
  - **Seleção duplicada "United States"/"USA"**: mesma seleção com 2 `team_id` (663, `external_id` real da football-data.org, usada na Copa do Mundo 2026; 847, sem `external_id`, criada do zero na sincronização da Copa America 2024 por NÃO ter reconhecido "USA" como o mesmo nome de "United States" — falha oposta ao caso Chile/Venezuela, nomes sem token em comum, mesmo padrão já documentado pra "Turkey"/"Türkiye"). Corrigido: jogos/stats da Copa America realocados pra 663 (o ID com `external_id` real), 847 apagado depois de confirmar zero referência restante em qualquer tabela.
  - **Nome duplicado em `vw_equipes_completo` (48 das 244 instituições cadastradas, praticamente todas as seleções)**: a view mostrava cada seleção 2x (ex.: "Espanha" e "Spain" como se fossem equipes diferentes, mesmo `equipes.id`). Investigado ANTES de apagar qualquer coisa: `instituicao_nomes` tem um design de propósito pra múltiplos nomes por instituição (`tipo`: original/transliteração/localizado, `idioma`), não é dado duplicado por engano — o problema real era só a `view` (`vw_equipes_completo`) filtrar por `formalidade='popular'` sem também filtrar por `tipo`, causando fan-out toda vez que existia nome original (português) + localizado (inglês) pra mesma seleção. Corrigido só a `view` (adicionado `AND n.tipo='original'`, confirmado que as 244 instituições têm exatamente 1 nome original+popular cada, sem exceção) — nenhum dado apagado, os nomes localizados em inglês continuam intactos pra uso futuro (i18n).

- **Migração pro Firebase Hosting (front-end só, opcional) + tela "Rodada Atual" do Brasileirão + backfill de odds históricas + correções no Model Benchmarking (23-26/07)**:
  - **Firebase Hosting**: front-end pode ser publicado no Firebase Hosting (projeto `agilsgh-65463878-f64e7`, `npm run deploy:firebase`) como alternativa ao Vercel — as 12 funções `api/*.js` e os crons continuam exclusivamente no Vercel (Firebase Hosting não roda esse código). `src/utils/apiUrl.js` + `VITE_API_BASE_URL` fazem todo `fetch('/api/...')` virar URL absoluta pro Vercel quando buildado em modo `firebase`; `api/_lib/cors.js` libera os domínios do Firebase Hosting. Vercel continua sendo o destino padrão, sem mudança de comportamento no build normal.
  - **`/rodada-atual` (nova, `src/pages/RodadaPrevisoes.jsx`)**: jogos do Brasileirão rodada a rodada com odd justa dos 13 modelos do Model Benchmarking (1X2 + O/U 2.5) lado a lado com a odd real da Pinnacle. "Rodada atual" não existia como conceito — calculada como a primeira rodada (ordem crescente) com menos da metade dos jogos finalizados, evita confundir rodada "quase encerrada" (jogos adiados reagendados) com a atual de verdade.
  - **Backfill de odds históricas do Brasileirão (`?tarefa=odds-historico[-descobrir]&liga_id=1`, novo em `api/model-maintenance.js`) — 177/177 partidas das rodadas 1-18 importadas (1X2 + O/U 2.5, Pinnacle/bet365/betano)**, com **4 bugs reais encontrados e corrigidos testando em produção** (nenhum era óbvio de antemão): (1) query de jogos locais sem filtro de `season`/`round IS NOT NULL` estourava o corte de 1000 linhas do PostgREST (mesma classe de bug já documentada) e escondia os jogos certos; (2) `/v4/historical-odds` da OddsPapi devolve o histórico de movimento de linha INTEIRO (~3,9MB de JSON por partida), a maioria dos pontos é cotação AO VIVO durante/depois do jogo — pegar só o `createdAt` mais recente pegava o preço quase decidido pelo placar, não o fechamento pré-jogo; corrigido filtrando só pontos com `createdAt <= horário do apito`; (3) 6 dos 20 times do Brasileirão têm apelido sem raiz comum com o nome da OddsPapi (CA Mineiro/Atletico Mineiro, Chapecoense AF/Chapecoense, Coritiba FBC/Coritiba FC, Grêmio FBPA/Gremio FB Porto Alegrense, RB Bragantino/Red Bull Bragantino, SC Corinthians Paulista/SC Corinthians) — `ALIASES_ODDSPAPI` novo dentro de `nomesBatem`; (4) bet365 às vezes grava `price:0` pra mercado suspenso — sem filtrar `price<=1`, virava odd "real" de 0.000 (196 linhas afetadas, apagadas manualmente, não reprocessadas por já estar perto do teto da cota mensal — ~63 partidas ficam sem bet365/O-U2.5 especificamente até uma próxima rodada de cota). Endpoint por fixture individual (não por torneio, diferente do sync ao vivo) — custo real de cota é alto (~180 chamadas pra uma temporada), avisado ao usuário antes de rodar.
  - **Probabilidades dos modelos de ML não somavam exatamente 100%** (pedido do usuário): a causa NÃO era o algoritmo (CatBoost/XGBoost/LightGBM já usam classificador multiclasse nativo — softmax sempre soma 1 — e a calibração Platt/Isotonic já renormaliza explicitamente após o ajuste one-vs-rest) — era o `round(..., 5)` aplicado a cada seleção de forma INDEPENDENTE na hora de persistir em `predicoes` (`scripts/rodar_predicoes.py`, `montar_linhas_predicoes`/`montar_linhas_predicoes_over_under25`), sem reajustar a soma depois. Corrigido com `renormalizar_arredondado` (absorve o resíduo do arredondamento na MAIOR probabilidade, técnica de maior resto) — cobre cron diário e backfill histórico de uma vez (as duas funções são reusadas por `backfill_predicoes_historicas.py`).
  - **Curvas de aprendizado (Treino/Validação/Teste, pedido do usuário) — infraestrutura nova, só no backtest manual**: não existia nenhum histórico de loss por iteração do boosting antes (só métrica final do Test Set em `model_benchmarking_backtest`). `scripts/modelos_ml.py` (`treinar_catboost`/`treinar_xgboost`/`treinar_lightgbm`) ganhou `val_df`/`test_df` opcionais — quando passados, usa `eval_set` nativo dos 3 frameworks (testado empiricamente contra as versões EXATAS do `requirements.txt`, formato de retorno confirmado, não documentação): CatBoost usa `get_evals_result()` (`learn`/`validation_0`/`validation_1`), XGBoost usa `evals_result()` (`validation_0/1/2` posicional), LightGBM usa `eval_names` explícito. Só `scripts/backtest_kelly.py` (`tunar_treinar_e_calibrar`) passa isso — captura a curva da config VENCEDORA do grid search durante o próprio treino em Train (sem custo extra de fit), nunca do refit final em Train+Val (esse não tem Val separado pra comparar). O cron diário de produção (`rodar_predicoes.py`) nunca passa `val_df`/`test_df`, então nunca gera curva — decisão do usuário, evita custo de CPU desnecessário no runner gratuito. Nova tabela `model_benchmarking_learning_curve` (delete-e-regrava, mesma disciplina de `model_benchmarking_backtest`), nova seção "Curva de aprendizado" em `PainelInfoModelo.jsx` (`src/components/CurvaAprendizadoModelo.jsx`, SVG desenhado à mão, paleta categórica validada contra o fundo escuro do app via skill de dataviz do projeto).

## Ambiente
- Supabase: `cgurxgfdmpmsnrshqycx`, service_role key trocada em algum momento por exposição prévia — confirmar qual está em uso
- football-data.org token: `eff3d4a516b74d96a357738d6e2a987f` (⚠️ foi exposto em screenshot antes, considerar trocar)
- Pasta local: `C:\Users\jbati\AntiGravity\quant-pred\quant-predictor\arquivos_do_claude\` (mudou de path em algum momento da conversa, confirmar qual é atual)
